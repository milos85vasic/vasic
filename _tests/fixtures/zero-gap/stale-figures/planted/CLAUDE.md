# Lab carrier (planted corpus for the stale-figures sweep class)

The fleet declares **3** widgets, re-measured today.

`--run-proofs` now exits 0 — re-measured 2026-09-03 at
  **65 PASS / 0 FAIL / 0 DEBT / 0 UNDET / 0 NOTE**. The reading this bullet

The registry holds 42 checks as of the last session.

An earlier figure of 17 checks is WITHDRAWN and kept as a record.

The mirror line: the index carries 7 anchors.

The index file is 1,804,638 bytes on disk.

The audit gate exits **1** on this tree.
