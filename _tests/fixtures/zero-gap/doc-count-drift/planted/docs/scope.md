# Scope

Affected: 2 files under `data/`; see the list.

The bare `notes.txt` (9 lines) name is ambiguous.

In `data/`, 4 `*.md` files exist.

In `data/`, the e2e folder has 6 specs.

A code comment: # 30 submodules in some other .gitmodules file.
