# scratch carrier
- `docdrifttool` is NOT INSTALLED on this host, so gate gamma exits 2.
- `presenttool` is NOT INSTALLED (WITHDRAWN: it was true when written).
- `ghosttool` is NOT INSTALLED, and gate alpha exits 2.
