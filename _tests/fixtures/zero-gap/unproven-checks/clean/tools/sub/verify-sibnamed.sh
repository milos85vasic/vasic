#!/usr/bin/env bash
# fixture: gate-like script with no proof arm of its own; its paired proof is a SIBLING script
# (prove-three-valued.sh) that names it inside the file
[ -s README.md ] || exit 1
exit 0
