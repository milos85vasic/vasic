# SDD ledger — plan: specs/010-zero-gap-verified-closure/tasks.md
# Executed via /speckit-superspec-execute (operator directive 2026-09-25). Rulings are ledgered as `ruling:` entries.
feature: 010-zero-gap-verified-closure
started: 2026-09-25
base_commit: 5bcc953
current_phase: 3
phase_status:
  phase_1: complete
  phase_2: complete
  phase_3: in_progress (operator approved 2026-09-26 with 'continue everything and fan out subagents')
tasks:
  T001: {status: complete, result: "verifier REFUSES our schema (unknown field); two-file store adopted (D6a); constitution gates 8/8, 7/7, 16/16 PASS"}
  T002: {status: complete, result: "canonical validator rc 0 and umbrella validator rc 2 (pre-existing roster gap) identical before/after additive columns + item_verdicts on a copy"}
  T003: {status: complete, result: "run-proofs 33m24s, 1.13 GiB, rc 1: 120 PASS / 2 FAIL (provider-ci selftest, workable-items proof control) / 1 UNDET (content-boundary proof timeout 900s); tree unchanged; ceiling accepted"}
  T004: {status: complete, result: "sibling check; 33 candidate sites (15 non-printf producer); printf-exemption policy open"}
  T012: {status: complete, result: "lib reviewed: round 1 fixed I1-I6 (CRLF, chmod/symlink/nested-repo, submodule state, set -e, empty combine, prover rc2) + minors; re-review ADDRESSED, no Critical/Important; prover 30/30 in a quiet clone (live M6 returns rc 2 while others edit)"}
  T006: {status: complete, result: "gap migrate additive+idempotent; partial-migration => exit 2 naming DDL"}
  T007: {status: complete, result: "V-G1..V-G12, --as-of, deterministic output; accepted after 4 fix rounds"}
  T008: {status: complete, result: "gap subcommands incl. verdict; closure gate hardened over rounds 1-4; 278 tests pass + 1 pre-existing baseline failure"}
  T009: {status: complete, result: "independent review + 4 scoped re-reviews; final verdict ACCEPTABLE to build on; residual: closed = structurally consistent + self-recorded digests until T068 chain binding"}
  T010: {status: complete, result: "fixtures corpus incl. golden-bad per rule"}
  T013: {status: complete, result: "two-file evidence adapter (verifier-native chain + sidecar bound by artifact_path); argv policy in one process; credential canary never on disk"}
  T014: {status: complete, result: "chain+anchor verifier; strict tracked-anchor default; witness fetch from fixed origin into a private ref; shared/exclusive locking; proofs 37/37 + 77/77"}
  T015: {status: complete, result: "independent review + 4 scoped re-reviews; final: ACCEPTABLE, 0 Critical/Important, 5 Minor"}
  T016: {status: complete, result: "the zero-gap scripts were registered by their own tasks (R5 requires it at creation); verify-check-registry.sh rc 0, 86 PASS"}
  T005: {status: complete, result: "baseline captured in baseline/ (path-normalised); workable-items rc 2, claim ledger rc 0, manifest pins rc 0"}
rulings:
  - "Pre-flight: Phase 1 tasks T001-T004 write only research findings; they return text and the controller appends addenda to research.md (avoids a shared-file write race). T005 is run by the controller (mechanical capture, new baseline/ directory)."
  - "Execution on main (no worktree): project practice is direct commits via the `commit` wrapper; the operator has run every prior step this way. Cost if wrong: a bad commit lands on main and must be fixed forward (no force-push)."
  - "Ruling: Phase 1 tasks are research, not code; instead of a reviewer per task, ONE independent review of all Phase 1 outputs runs at the phase checkpoint. Cost if wrong: a wrong finding could shape Phase 2 design before review; mitigated by the checkpoint gating Phase 2."
  - "Ruling: T001 changed the plan (two-file evidence store). Applied to research.md, data-model.md, contracts/evidence-record.schema.json and tasks T013/T014 in this same phase rather than deferring, because T012-T014 depend on it. Cost if wrong: schema churn before any code exists."
  - "Phase 1 review (fresh opus reviewer): NO-GO on doc defects only; fix round 1 applied: argv typed as array, anchor stored in upstream format {head_digest, entry_count, anchor_strength=policy}, per-commit forward-only git-history check, adapter invariants list, Go shim for ExecRow.ToRecord (library-only), V-G12 orphan item_verdicts rule, stale mirror wording removed, T004 counts stated as regex hits. Re-review of the fix diff pending with the T003 result."
  - "Phase 1 fix round 2: scoped re-review found ADDRESSED for F2,F3,F5,F6 and PARTIAL on F4 (stale D6 wording in research.md lines 85-87). Fixed by the controller as a doc-only wording edit rather than a new dispatch, because the fix is three lines of prose with no code. Cost if wrong: a stale word survives; grep-verified below."
  - "T009 review (fresh opus): spec FAIL / quality NOT READY — C1 verifier evidence never parsed, C2 re-close on stale evidence, I1 nondeterministic validator output (map iteration), I2 environment faults exit 1 not 2 (read-only, locked DB), I3 frozen-cycle record editable undetected, I4 live-register migration would flip G5 rc 2→1. Fix round 1/5 sent to the original implementer (RED-first). Contract amended for `gap verdict` and the extra flags; live-register migration deferred to T047 (ledgered, also in contracts/register-cli.md)."
  - "T009b scoped re-review of store fix round 1: C1,I1-I4,minors ADDRESSED; NEW Critical N1 (reopen cut backdating: `gap reopen` accepts a failing record older than the latest closure), Important N2 (verifier evidence may be the GREEN record itself), N3 (reviewer evidence has no time bound). Fix round 2/5 sent to the original implementer."
  - "T015 review of T013+T014 (fresh opus): 0 Critical, 5 Important (chain accepted without final newline then corrupted on append; default must be strict git-history check; canary credential left in .incoming after SIGTERM/SIGKILL; password split across two argv words leaks; kept recorder rows undecodable by upstream DecodeExecRows) + 7 Minor. Fix round 1/5 sent to the original implementer."
  - "Ruling: STORE LOCATION DECISION (from T015 finding 6/2) — the evidence chain, sidecar and streams live UNTRACKED under .remember/logs/zero-gap/evidence/ (git-ignored) and the ANCHOR is a TRACKED file docs/zero-gap/anchor.json (no paths). Reason: sidecar cwd/stream_ref would otherwise embed absolute paths (audit-hardcoded-paths), and a tracked anchor gives the git-history verifier its only independent witness. Cost if wrong: anchor changes require explicit commits; a mislocated store is a path change, not a redesign."
  - "Upstream defects found (report, do not patch; register as classified third-party items in T033/T034): the constitution's recorder writes `command` unescaped (rows with quotes/tabs are not valid JSON, a newline arg splits a row) and silently records stream_redacted=false when its scanner cannot load; its credential scanner takes 94.6 s on one 70 KB line (0.14 s when split); the canonical workable-items `close` wipes all 14 gap columns and skips closure checks."
  - "T009c (round-2 re-review): N2/N3/minors ADDRESSED as specified; N1 NOT fully addressed (A1 bypass inside the 5-minute skew); new Important I1 (cut must cover every cited record ts; ban reuse of GREEN/verifier files), I2 (validate vs commands disagree on future dates), I3 (no digests stored at close ⇒ post-close evidence swap undetected, FR-016), Minors M1-M3. Fix round 3/5 sent to the original implementer."
  - "Ruling: CHAIN-BOUND CLOSURE EVIDENCE is NOT implemented in the store rounds; it is made an explicit requirement on T068 (added to tasks.md). Why: it depends on the T013/T014 evidence chain still in fix rounds, and forcing it into T008 would couple two unreviewed components. Cost if wrong: until T068 the register's 'closed' is structurally consistent and self-asserted only (an unsigned verifier record can be forged from a copy of the GREEN); FR-016/FR-018 must not be claimed met until T068 — recorded in tasks.md and to be repeated in CONTINUATION."
  - "Store rounds: T009 (round 0) spec FAIL/quality NOT READY (2 Critical); round 1 fixed C1/C2/I1-I4 -> re-review found N1 Critical + N2/N3; round 2 fixed those -> re-review found N1 still open (skew window) + 3 Important; round 3 (chain binding ruled OUT to T068) -> re-review found partial-migration gate-off + FIFO hang + RED reuse; round 4 (fresh implementer, most capable model) -> re-review: ALL ADDRESSED, store ACCEPTABLE. The loop stayed within the 5-round cap."
  - "minor (deferred) T008: reviewer evidence_sha256 can be stored NULL if the file changes between Stat and hash (refuse rc 1 when the digest is empty; same for close's research sha256)"
  - "minor (deferred) T008: future-dated history rows (created_at 2030) accepted rc 0 — should be a V-G9 finding"
  - "minor (deferred) T008: reviewer on_date unbound (may precede the GREEN day)"
  - "minor (deferred) T008: duplicate JSON keys accepted (last wins)"
  - "minor (deferred) T008: parallel-close losers exit 2 'touched 0 rows' instead of rc 1 'already closed'"
  - "minor (deferred) T008: O_NOFOLLOW protects only the final path component (parent-dir swap between EvalSymlinks and open); use openat2(RESOLVE_BENEATH); add a //go:build unix tag"
  - "Accepted residual (from the store's own notice): anyone with direct SQL write access can rewrite evidence and its digest, backdate within the same day, or strip the schema and rename the zero-gap: history prefix (then the register reads as unmigrated) — undetectable until T068 chain binding; FR-016/FR-018 NOT claimed met by the register alone."
  - "Evidence stream rounds: T015 (round 0) 0 Critical / 5 Important -> round 1 fixed (chain byte canonicity I10, strict git-history default, canary cleanup, argv secret scan, exec_rows decodable) -> re-review: 3 Important (argv secret shapes incomplete, verify/preflight unlocked, amended commit passes history) -> round 2 fixed -> re-review: chain/lock/anchor integrity ACCEPTABLE; NOT yet a secret barrier or a tamper witness (I1 partly: word-level shapes, quoted script args; NEW Important: witness forgeable via update-ref/branch -u); round 3 sent."
  - "Process slip recorded (§11.4.174): the T015c reviewer ran `pkill -f 'sleep 60' -u <me>`, which killed its own tool shell and could have signalled other processes of the same user. Post-check: all four services, the workshop container and this session's other work were intact. Guidance now in every brief: never signal by pattern; signal only pids you started."
  - "minor (deferred) T014 N1: percent-encoded query keys (`?%74oken=X`) get through the argv policy and redactURL — decode %HH in normalizeArg and redactURL"
  - "minor (deferred) T014 N2: names outside the list pass (`--auth X`, `--creds`, `--pat`, `NPM_AUTH=`, `X-Auth:`) — add auth/creds segments or list under NOT caught"
  - "REQUIRED BEFORE T068 (from T014 N3): the witness fetch recurses into populated submodules, triggers auto-maintenance, and remote.<r>.uploadpack from the repository's own config executed a marker; header wrongly says upload-pack runs 'on the remote's side' and history.go:384 comment 'never executes anything on this host' is false. Fix: --no-recurse-submodules --no-auto-maintenance -c remote.<r>.uploadpack=git-upload-pack; correct both texts. Pinning core.sshCommand/credential.helper would break operator setups — keep as documented residuals."
  - "minor (deferred) T014 N4: `--build-bins` pointed at the module dir drops an un-ignored zg-chain binary"
  - "minor (deferred) T014 N5: the exempt row's fact_sha256 covers only the adapter script, so a change to argvpolicy.go is not drift"
  - "Accepted residual T014: name-less argv values (redis-cli -a X) and computed/obfuscated values are not caught; the upstream scanner is the only guard on stdout/stderr; global/repo git config (sshCommand, insteadOf, credential.helper) and ssh env still run programs during the witness fetch; -P/-p refusals need a wrapper; records must run one at a time or with ZG_LOCK_TIMEOUT above the longest check; locking is Linux-only. Upstream limitations to REPORT: recorder writes `command` unescaped, silently records stream_redacted=false when its scanner cannot load, forks sed/tr/sed per argument (~28 s per 3000 args), credential scanner is 94.6 s on one 70 KB line and misses pass=/token=/short passwords/Bearer/URL creds."
  - "T017/T018 review (fresh opus): runner works (45/45 proof, 5x byte-identical live sweeps) but contract NOT yet safe for 17 parallel implementers — C1 commit protocol, I1 locations with spaces mis-parsed, I2 environment leaks (GIT_DIR, POSIXLY_CORRECT, locale), I3 control chars, I4 fingerprint blind spots, I5 budget/process groups/population sha, I6 six hand-listed or circular population_why rows, I7 fixture exclusion only documented. Fix round 1/5 sent to the original implementer."
  - "Ruling: CLASS-WAVE COMMIT PROTOCOL — class implementers append ONE registry row each (atomic append, 0 lost lines in 30x17 trials) and NEVER stage or commit; the controller makes ONE wrapper commit for the whole class wave after every script and row exists and verify-check-registry.sh is rc 0. Why: the commit wrapper stages the whole tree, and a per-class commit would ship other classes' rows before their scripts exist (R3 on HEAD) or scripts without rows (R5 blocks pushes). Cost if wrong: one large wave commit is harder to bisect; mitigated by per-class reports and the pre-commit registry check."
  - "Ruling (T081): the improvement-candidates population row was widened by the controller to add the `minor (deferred)` lines of progress.yml and Queued DB items (the class implementer had stopped with NEEDS_CONTEXT because the controller's prompt described a row the runner implementer had not written). Why: the programme's own deferred minors are existing-capability improvements, which FR-001/FR-025 admit. Cost if wrong: extra candidate findings the operator can decline at T038; the class is re-dispatched with the widened row."
  - "Class-wave reconcile list (apply in the class review fix rounds, serial): (1) known-open-decisions must EXCLUDE progress.yml `minor (deferred)` lines (improvement-candidates owns them; 10 of its 24 live findings were double-counted developer deferrals) and its sweep-classes.tsv row wording must drop the remote-sync/provider-CI network probes (pointer-drift owns remote probing); (2) unregistered-scripts scope is UMBRELLA-ONLY (22 submodules carry ~1,900 tracked *.sh and no registry of their own): the row text 'owned submodules' must be corrected and the limit documented; (3) docs/zero-gap/ (README, sweep-classes.tsv, toolchains.tsv, guarded-commands.tsv) MUST be in the wave commit because findings cite those files; (4) fixture exec bits are set on disk and preserved by the wrapper's git add."
  - "Class-wave reconcile list (cont.): (5) stale-figures volume — 2676 live findings (1 high, 418 medium, 2257 low; docs/setup-agents-wizard/LUMEN-STORE-INVENTORY.md alone 809, mostly hex ids): the unledgered LOW tier becomes ONE aggregate finding per document with a count (`figures:<doc>`), ledger-verified FALSE figures stay per line, and hex-id-only lines are not figures; (6) stale-figures vs doc-count-drift overlap: BOUNDARY RULING — stale-figures owns the four carriers, CONTINUATION.md, README.md and ledger anchors; doc-count-drift owns docs/** and module docs; stale-figures' docs/**/*.md scan is removed and doc-count-drift's population text must say so; (7) sweep-classes.tsv rows for both classes are edited serially by the controller after the class reviews."
  - "Ruling (private-in-public corpus): the planted corpus MUST NOT hold secret-shaped strings AT REST (an AWS-key-shaped string, a private-key header line, a high-entropy token, a URL password) — GitHub push protection or another secret scanner may refuse the wave push, and the commit history is permanent. Fix in the class review round: planted/ stores documented placeholder tokens (e.g. `@@ZG-AKIA@@`, `@@ZG-PRIVKEY-HEADER@@`) and the class's `--corpus` mode expands them in a SCRATCH copy in $TMPDIR at scan time (documented, tested: the unexpanded file yields nothing, the expanded copy yields the planted findings; the runner's recall engine is unaffected because it calls the class with --corpus). Also: the constitution's credential scanner lives at submodules/constitution/scripts/hooks/credential_scan_lib.sh (my brief said scripts/gates/lib/ — wrong). Cost if wrong: a slightly more indirect corpus."
  - "Real defects surfaced by classes so far (feed the closure wave; none fixed): live-vs-source — workshop served build 185fb15-20260925T090616Z vs source HEAD e076fa6 (1 commit ahead), container started before that commit, and ai_interviewing /api/health carries NO source commit (the running aicur is stamped with the ENCLOSING repo's vcs.revision 923779e06397, so the running ai build cannot be tied to its source; cause UNCONFIRMED — Go build stamping picks up the enclosing repo because the submodule's .git is a file); build-if-missing — ai_interviewing/platform/scripts/start.sh:17 and ingest.sh:4, _tools/deploy-langs.sh:665, scripts/verify-workable-items.sh:128, submodules/llms_verifier/scripts/generate-ultimate-opencode.sh:13, _tests/export/self-validate.sh:30; guard-gaps — the PreToolUse guard also lets `git reset --hard` through (sixth gap beyond the five predicted); vacuous-gates real hits — scripts/test-setup-agents-wizard.sh:1543, scripts/verify-workable-items.sh:278, scripts/continuation-check.sh:207 (+ verify-content-boundary-judgement.sh:411, zero-gap-evidence-chain.sh:768, zero-gap-sweep.sh:884); untracked-blind-window — audit-hardcoded-paths.sh:433, audit-environment-assumptions.sh:836, verify-provider-ci.sh:1134, verify-shell-continuations.sh:106; unregistered-scripts — 12 unregistered executables (.specify/scripts/bash/*, _tests/**, upstreams/GitHub.sh) and 9 R5 blind-spot scripts under _tools/; stale-figures — README.md:3 zero-findings badge; carriers still say --run-proofs 65 PASS."
  - "Ruling (unsealed-evidence): confirmed — population = every check row of scripts/check-registry.tsv plus the pre-push gates, and `store:`/`anchor:` are population items (absence findings need a location). T038 GROUPING NOTE: the 54 per-check unsealed findings (and the 8 gates) share ONE root cause (nothing feeds sealed records: T065), so register seeding groups them under a single item per root cause with the member list attached, instead of 62 items; same treatment for other classes whose findings share one root cause (stale-figures unledgered tier, unproven-checks name-heuristic hits). The runner keeps per-finding locations; grouping is a T038 register decision."
  - "Class-wave reconcile list (cont. 2): doc-count-drift (16/16 proof; live rc 1: 16 high / 27 medium / 1484 low; seed reproduced 23/27/20 vs 101/94/47) needs in its review fix round: (a) STOP covering the umbrella carriers, CONTINUATION.md and README.md (stale-figures owns them per the boundary ruling); keep module carriers/docs; (b) collapse LOCKSTEP carrier copies (CLAUDE/AGENTS/QWEN/GEMINI in one module) into ONE finding with an `also in` list (12 high for 3 claims today); (c) a tracked exemption list docs/zero-gap/point-in-time.tsv (session reports, ledgers, dated spec notes whose figures were true when written) with a reason per row — no invented exemptions in code; (d) the 1484 unmeasurable LOW findings become per-document aggregates (`figures:<doc>`); (e) locations `doc:line` are unstable across insertions — add a content-anchored secondary key (the claim text hash) to the description so T038 can dedupe across cycles; (f) the sweep-classes.tsv row text must match the implemented union (README/CLAUDE/AGENTS/QWEN/GEMINI .md, docs/**/*.md, specs/**/*.md in the umbrella and owned checked-out modules; glob form `N `glob` word`; PASS/FAIL splits not implemented)."
  - "Process note: the session scratchpad is shared by all parallel implementers; files with the same name collide (one class's first report draft embedded another class's transcript; another moved its harness). Future parallel waves must give each implementer a private scratch subdirectory (/tmp/zg-impl-E/<id>/ for scratch, not only reports)."

  - "Class review rev-register (opus): known-open-decisions and improvement-candidates both ACCEPTABLE-AFTER-FIXES. Fix list for the class fix round. KOD: C1 corpus-mode DB findings must use evidence_ref docs/workable_items.sql (corpus has no .db; refok rejects, recall UNKNOWN) — and add a proof case that validates every output line with the runner refok/canon rules; I1 (ruled, REQUIRED) drop `minor (deferred)` and `REQUIRED BEFORE` from the progress.yml pattern, live precision was 12/25=48%; I2 refuse non-regular/symlink source as COULD-NOT-INSPECT (FIFO hangs, rc 124); I3 newline in tracked path splits records — encode inside Python; I4 RESOLVED must not match `unresolved` (use (?<!un)\\bresolved\\b, plant a case); minors: copy -wal with the .db, proof case POPULATION-SHA == emit. IC: C1 same evidence_ref defect (SYN-001/002); I1 measurable target picks the earliest figure (usually current state, ~12% real targets live) — prefer comparator/`target:` matches, label other figures `figure in source (not a target)` and seed as target MISSING, fix proof C3a to `under 30 seconds`; I2 population deviation — the row says each Operator-blocked item / deferred line / every Queued item is a member, code has 5 file-level tokens (INSPECTED=5, DB changes never move POPULATION-SHA) and a vocabulary filter keeps 19 of 203 Queued: RULING pending controller — move to item-level tokens with the filter documented as the decision rule (preferred), else reword the row; minors: plant header-row limitation case and later-regex-earlier case, expect.tsv comment says 30 seconds but class emits 90, refuse relative --root, T038 needs a KOD/IC dedupe rule. Load-bearing for T036: KOD C1/I1/I2/I3, IC C1/I1/I2."
  - "Class review rev-gates (opus): coverage-gaps ACCEPTABLE; vacuous-gates, unproven-checks, unregistered-scripts ACCEPTABLE-AFTER-FIXES. Fix list. vacuous-gates: CRITICAL zero-fail-pass precision is 0.20 live (10 hand-read, 2 real; 9 hits are the `[ $fail -eq 0 ] && exit 0` line in sibling zero-gap proof harnesses incl. its own :333, plus fixed-case proofs at verify-content-boundary-judgement.sh:411, zero-gap-evidence-chain.sh:768, verify-workable-items.sh:278, continuation-check.sh:207 where zero executed cases is impossible) — exempt a verdict whose counter is only incremented by straight-line proof-case calls with no data loop, re-measure and restate precision in the header, CORRECT the progress.yml real-defects list (keep test-setup-agents-wizard.sh:1543; drop the four fixed-case proofs; add audit/zero_findings_sweep.sh:525 missing scanroot skipped with continue); plant a loop-procsub case; give the here-doc near-miss a verdict-bearing loop body; minors: multi-line gates (gate_1 Go, gate_6 Playwright) become COULD-NOT-INSPECT 'non-shell gate', live_sum hash file contents, registered symlink followed outside root. unproven-checks: plant cases for exempt-missing-file (:363), stub arm on uncovered scripts (:377), proof-kind-outside-vocabulary, malformed dates (two survivors confirmed); recognise or admit two FP families ([normal|mutate] positional mode, sibling proof naming its target inside the file); minors INSPECTED prints token count not items, header says newline paths refused but they split, header must state it executes every registered entry point with --root /nonexistent. unregistered-scripts: ALWAYS print INSPECTED and POPULATION-SHA (missing tracked file exits without them, :259-262); :223 grep|grep|grep -q under pipefail (forbidden) — here-string/temp file; --prove-failure lacks live-tree byte-identical assertion; minors: grep -F substring x.sh matches x.sh.bak, header claims runner flags zero-gap-class scripts but R5 does. coverage-gaps: population deviation (implemented subject x all kinds + dangling refs + n/a without reason + missing rows vs tsv 'gap cells') — RULING: accept the broader population and correct the tsv row text; minors: cite the 12 kinds source and single-source with T052, planted ../outside.sh never exercises safe(), `gap:<suffix>` status becomes undetermined instead of finding. Cross-class RULING needed: ONE exclusion classification shared by unproven-checks and unregistered-scripts (derive third-party trees from helix-deps.yaml; decide .specify/ — unregistered-scripts reports .specify/scripts/bash/* (6), unproven-checks excludes all .specify/) — controller rules: derive from helix-deps.yaml for both, .specify/scripts/bash is vendored spec-kit tooling and is EXCLUDED as third-party for both. Load-bearing for T036: vacuous-gates zero-fail-pass, unregistered-scripts INSPECTED/SHA + pipefail, tsv row texts, shared exclusion."
  - "Class review rev-build (opus): all four ACCEPTABLE-AFTER-FIXES. live-vs-source: plant stack-no-identity-noexe.svc + expect row `:stamp` (untested branch line ~277, mutation survived); podman 5.7 `.State.Health` is a pointer — template must be `{{if .State.Health}}{{.State.Health.Status}}{{end}}` (UNCONFIRMED failure, cheap guard); run `go version -m` with HOME/XDG_CONFIG_HOME in the scratch dir (go telemetry counter writes to real HOME); RULING: a stopped defined service stays COULD-NOT-INSPECT (rc 2 is honest) and the tsv row text is corrected to say so. build-if-missing: FALSE NEGATIVE — `local timestamp=` matches /stamp/ and suppresses run_all_providers_challenge.sh:79; fix: strip inline comments and echo/printf/log lines before fresh_line, require stamp as a compared operand, treat `rev-parse HEAD` as fresh only when compared; FIFO/symlink tracked file hangs (rc 124, leaves zg-bim.* tmp) — skip non-regular, COULD-NOT-INSPECT; population must exclude third-party trees (10 start scripts under submodules/qa/tools/opensource/** — use config/covenant_propagation_exclusions.tsv / helix-deps.yaml, same shared exclusion ruling as unproven-checks/unregistered-scripts); minors: severity by consequence (fixtures/gates lower than start scripts), header 'tracked files only'. missing-toolchain: FAILS OPEN when perl is broken (derived population silently dropped, findings 10->3) — need-tools check for perl/iconv/awk/git (missing => rc 2) and check xargs perl status; RULING: keep derived: and docs-drift as additions to the catalogue population, but emit derived: at severity high only when the probe is followed by exit 2/return 2/undet call, else low, and correct the tsv row text; minors: missing iconv reported as 'not valid UTF-8', tool name `..` yields invalid token. untracked-blind-window: `git ls-files -co --exclude-standard` falsely flagged (covering regex -[a-z]*o); `echo ... --others ...` falsely marks covering (move echo/printf skip before the covering check); proof too weak (mutation nopv survived --prove-failure; compare full expect.tsv set); minors: `..` entry point token -> registry:<line>, non-regular registered entry point -> COULD-NOT-INSPECT, `x=$(git ls-files | xargs stat)` missed, pre-push gates not in population though row says check and gate (RULING: add them or correct row — correct row text). Load-bearing for T036: build-if-missing stamp FN + FIFO + exclusion; missing-toolchain fail-closed + derived severity; untracked-blind-window -co/echo order; live-vs-source podman nil-health guard."
  - "Class content-boundary-rows (T023) DONE_WITH_CONCERNS: proof 24/0 (RED against stub 3/21), live rc 2 COULD-NOT-INSPECT (no cached gate result), corpus recall 1.0. The gate itself takes ~36 min (2170 s) and 760 MB, so the class never runs it live: it reads .remember/logs/zero-gap/content-boundary-rows.{json,corpus} and trusts it only if five checks pass (shape, stable tree, root+provider fleet, manifest digest, recomputed manifest equal). Ruling: ACCEPT the cache design (a 2170 s gate cannot fit the 900 s class budget); the cache refresh is an explicit ONE-TIME step in T036 run on a frozen real copy — NOT during the wave; until then this class reports rc 2 in every sweep and T036 must state that verbatim. Ruling: accept the added `gate:scripts/verify-content-boundary.sh` population token (empty population is forced to COULD-NOT-INSPECT otherwise) and correct the tsv row text. Grouping repo+2 dirs, count band in description not location: accepted. Matched private text never stored (refresh strips .match with jq). Open review items: independent class review (assign to rev-register-2), refresh command must use a scratch clone so the tree cannot move, and the class re-implements the gate file enumeration so a gate rule change makes every cache stale (rc 2, safe)."
  - "Class review rev-safety (opus): unsealed-evidence ACCEPTABLE; private-in-public and guard-gaps ACCEPTABLE-AFTER-FIXES. private-in-public fix list: (1) REQUIRED (ruled) planted/secret.md:3-6 still stores AKIA/PEM/high-entropy/URL-password at rest — replace with @@ZG-...@@ placeholders expanded into a $TMPDIR copy under --corpus, prove unexpanded yields nothing and expanded yields the 4 locations; (2) load-bearing: false CRITICAL content-boundary finding at specs/010-zero-gap-verified-closure/progress.yml:63 (a YAML double-quoted ruling naming ai_interviewing/platform/scripts/start.sh) — in long_quote reject a quoted span that contains the private path or is the whole YAML/JSON scalar, add clean controls (YAML ruling naming a private path; backticked private path plus a quoted scope phrase); (3) precision measured 0/5 security, 0/5 content-boundary, 1/5 guessing on specs/002-009 (Go test names pass the entropy rule) — exclude identifier-shaped tokens, plant as clean controls, restate header precision lines (lines 70 and 49-52 say 0 hits, stale); minors: bare appears/possibly/apparently/could be/may have not in vocabulary and lines containing forbid/guess skipped wholesale, ENTTOK literal 34-char string at line 490 (build at run time), name ending in newline mislabelled CNI (decode uses command substitution) and symlink read outside --root (report symlinks COULD-NOT-INSPECT); RULING: evidence streams named in the row are T062-owned ignored files — amend the row text. unsealed-evidence minors: 1 MB check id makes grep rc 2 read as unsealed (treat rc 2 as CNI, cap id length); U+202E in registry id printed raw so the runner rejects the whole class output (print encoded token). guard-gaps: LOAD-BEARING catalogue omits commands the governance docs name — poweroff, pkill -f, killall, rm -rf /, git filter-branch, git branch -D, git update-ref -d, git clean -fdx, git checkout ., git add . all returned rc 0 (allowed) from the live guard (data only, nothing executed) — add rows WITH sources to docs/zero-gap/guarded-commands.tsv (these become T038 register items); RULING: the hand CANDIDATES list at line 72 moves to tracked data (docs/zero-gap/guarded-commands.tsv candidate column) — the row forbids a hand list inside the class; minors: timeout needs -k (guard ignoring TERM ran 25 s under ZG_GG_TIMEOUT=2), clean corpus lacks an `allowed` row."
  - "Class review rev-docs (opus): stale-figures, doc-count-drift, pointer-drift all ACCEPTABLE-AFTER-FIXES. stale-figures: (1) CRITICAL confirmed still needed — reconcile 5/6 not applied: still scans docs/*.md and counts hex ids as figures, hand precision 6/11, 1140 of 2676 findings hex-first; (2) RULING: population narrower than the row (no `bytes` noun, no exit|rc N shape; 16/21 byte lines and 213/274 exit/rc lines missed) — ADD both shapes to the population rather than narrow the row, since rc figures are the most drift-prone claims in the carriers; (3) symlink to FIFO hangs, to /dev/zero reads to MemoryError (host-memory §12.6 risk), symlink leaving the tree echoes up to 48 chars from outside — skip git mode 120000 and non-regular, require realpath under BASE, report COULD-NOT-INSPECT; (4) three surviving mutations (malformed-row finding, duplicate-id finding, high severity mapping) — plant a 9-field row, a duplicated id, assert figure:CLAUDE.md:6 is high; (5) implementer report evidence is not its own (RED block is pointer-drift's, GREEN block another class's) — re-capture both; minors: line 206 grep -qxF under pipefail, lone CR shifts line numbers (header). doc-count-drift: reconcile (a)-(f) confirmed still needed (live still covers umbrella carriers AGENTS.md:1826 '13 submodules' vs 22; 16 high = 4x lockstep copies; 7/11 drift hits are point-in-time docs); (2) cost quadratic in paragraph size (50 KB 3.1 s, 100 KB 12.3 s, 200 KB 45.5 s; ~1 MB exceeds 900 s) — compute backtick positions once per paragraph and bisect; (3) same symlink handling; minors: rc-2 paths print no INSPECTED/POPULATION-SHA, failed --emit-population prints CNI on stdout (stderr), plant `~20 *.sh` and a dir holding .hidden.sh near-misses. pointer-drift: (1) proof checks locations only — three mutations survive (BEHIND/AHEAD swap, third-party low severity, push probes) — assert severity and direction per planted case; (2) untested branches: .gitmodules path with no gitlink, dep with no ref, absent helix-deps.yaml, remote missing declared branch, remote with no HEAD and no branch, pushurl tip differing from gitlink; (3) LOAD-BEARING 16 of 35 live findings are 4 lockstep carriers x 4 submodules — collapse to one per submodule with an `also in` list; minors: set GIT_ALLOW_PROTOCOL explicitly, report RED block is content-boundary-rows' transcript. Load-bearing for T036: stale-figures 1/2/3, doc-count-drift 1/2/3, pointer-drift 3. Also serial sweep-classes.tsv row edits (reconcile 7)."
  - "T017/T018 runner fix round 2 DONE_WITH_CONCERNS (implementer a044726…): SHELLOPTS re-exec under env -i, ZG_RUN /proc scan for setsid escapees, NUL/U+0085/U+202E/U+2028 rejected, evidence_ref validated (./, .., abs, invented, blank-split, non-normal), tracked-path locations, GOMAXPROCS/GOTOOLCHAIN allow-list, 200000 findings render in ~85 s, .service-registry/qa/ in default ZG_IGNORED_EXCLUDE, README rev 3 with class-implementer rules, contract text updated, data-model SC-stale-figures token fixed. Proof in a quiet scratch mirror: 118 passed / 0 failed; new tests failed on round-1 runner; 5x determinism identical; runner defect found+fixed (gawk aborts on a vanished /proc file -> nondeterministic 'left processes' report) plus bounded wait after KILL; 18/18 parallel runs identical. Live sweep in mirror: VERDICT UNDETERMINED rc=2 could-not-inspect=17 (no class scripts in the mirror). Documented residuals: descendant that clears env AND leaves the process group escapes the ZG_RUN scan; small pgid-reuse window between verify and signal; JSON render is a bash loop (millions of findings would need awk); BASH_ENV/ENV file is read by the runner's first bash before line 1. Scoped re-review dispatched (rev-runner2). Do NOT edit scripts/check-registry.tsv from the runner side: the wave commit owns it."
  - "Class review rev-cbrows (opus): content-boundary-rows ACCEPTABLE-AFTER-FIXES. Fix list (in load-bearing order): (1) CRITICAL a genuine cache from a NON-DEFAULT gate run (--no-names --no-short --window 30, 0 leaks vs 35 default) gives rc 0 false clean — refuse (COULD-NOT-INSPECT) unless the JSON-recorded parameters equal the defaults (windows.long=10, windows.short=5, short_line_max=9, names=1, direction.enabled=true, direction.status=ran); header states --allow and --name-* are not recorded so cannot be checked; (2) rows the gate skipped become a clean result (counts.not_indexed=1 for a newline-named file -> rc 0) — emit COULD-NOT-INSPECT on gate:scripts/verify-content-boundary.sh whenever counts.not_indexed>0 or untracked.files_unscanned>0; (3) forged caches pass (emptied leaks with adjusted counts rc 0; fleet cut to public repo with recomputed manifest rc 0) — re-derive expected fleet paths from .gitmodules + helix-deps.yaml and treat a missing path as CNI, check per-class counts sum to counts.leaks and equal row counts, and CHANGE header line 48 to 'detects staleness and accident, not forgery: the cache is a writable ignored file' (RULING: the cache is an honest-accident guard, never a tamper guard; tamper evidence belongs to the §11.4.268 chain when T068 binds it); (4) FIFO/symlink-to-/dev/zero tracked entry hangs xargs cksum (rc 124) — non-regular -> CNI naming it, wrap hashing in timeout; (5) correct the tsv row: gate: sentinel token, population items are path classes with gate class as location suffix, 'on the swept tree' means cache proven fresh, path-class rule (repo + 2 dirs), fleet comes from the cached fleet cross-checked against .gitmodules. Deferrable: two surviving mutations (exit-consistency case lines 418-423, backslash/nbad guard line 492) need cases; refuse a cache that still holds .match; `--root`/`--corpus` without value loops forever (shift 2) -> [ $# -ge 2 ]; unknown arg prints CNI to stdout under --emit-population; corpus gaps (no fingerprint-class row, no multi-source description, no umbrella '.' prefix, no newline-named file)."
  - "Runner round-2 scoped re-review (opus): SAFE-FOR-17-CLASSES yes WITH CONDITIONS. Round-1 findings I2/I3/I5/evidence refs/.service-registry/qa fixed with evidence (env/SHELLOPTS/BASHOPTS/exported-function injection neutralised, bidi/NUL rejected, setsid escapee ended, 4/4 mutations caught, 5/5 determinism); mirror battery was 117/1 not 118/0. Round 3 dispatched to the runner implementer: I-A remove the I6 catalogue-schema asserts (class-owned catalogues; blocks wave commit and would redden --run-proofs); I-B GIT_CONFIG_COUNT=2 to neutralise ~/.config/git/ignore+attributes (implicit files still dropped an untracked file from the population); I-C unseen class side effects (rm/cp -p of ignored files, writes to .git/hooks/pre-push and .git/info/exclude) — find -cnewer, diff the !! list, digest {hooks,info}; minors: SHELLOPTS=noexec documented, U+061C in BADSEQ, evidence_ref must be a regular file with realpath inside --root, not under .git/, not fixture; SIGSTOP-then-rescan-then-KILL for TERM-ignoring spawners; contract text nit; drop-ZG_RUN escapee documented residual (cgroup scope is the only containment; userns unavailable). Ruling: no HOLDS verdict is trusted for the T036 baseline until round 3 is green."
  - "Class fix round DONE (register): known-open-decisions proof 20/0, improvement-candidates 39/0, every fix RED->GREEN, 9 KOD + 13 IC mutations all caught, three live runs byte-identical, recall 1.0 each. Re-measured live: KOD 13 findings (was 26), hand precision 10/13 = 0.77 (was 12/25 = 0.48; FPs CONTINUATION.md:597 decided, :616 off-by-design, progress.yml:68 quoting the phrase); IC INSPECTED 216 (3 docs + 10 deferred lines + 203 Queued) with 30 findings, 7/10 real; only 1 derived target (>=20, a check floor) and 29 MISSING (15 with a labelled figure) — correct under the documented rule. Deferred minor: IC figure regex reads '16 of 2026' from a date (needs a (?!-) lookahead + test). CONTROLLER sweep-classes.tsv EDITS PENDING (serial, after all fix agents finish): (1) KOD population wording: 'every recorded operator decision point: carrier statements naming an operator decision (CONTINUATION.md newest §3 entries), tracked docs/OPERATOR-DECISIONS-*.md and docs/constitution-adoption/DECISION-*.md, progress.yml rulings and pending-decision lines (excluding minor (deferred) and REQUIRED BEFORE lines), and Operator-blocked items in docs/workable_items.db (read through a copy); remote probing belongs to pointer-drift'; (2) IC row: documents are document-level members, each minor (deferred) line a line-level member, every Operator-blocked and every Queued item an item-level member, the limitation vocabulary is the decision rule for which members are findings, KNOWN-LIMITATIONS*.md enumerated exactly; (3) T038 dedupe rule: an Operator-blocked item reported by both KOD and IC is ONE register item keyed docs/workable_items.db:<atm>, and KOD CONTINUATION.md:565 restates :550."
  - "Class fix round DONE (build/runtime): live-vs-source 13/0, build-if-missing 28/0, missing-toolchain 21/0, untracked-blind-window all cases held; every fix RED->GREEN, named mutations killed, recall 1.0 each, 3 live runs byte-identical. Live re-measured (hand-read): live-vs-source 3/3 real; build-if-missing 7/7 (1 high, 4 medium, 2 low; the run_all_providers_challenge.sh:79 false negative now found) + 1 CNI for tracked symlink submodules/qa/scripts/anti-bluff-scan.sh; missing-toolchain 9/9 real on the default PATH, 7 under the runner; untracked-blind-window 3/4 (FP: verify-provider-ci.sh:1134 while-read only classifies file names). Deferred minors: a comment containing 'untracked' counts as a stated limit (a known FN family; it is how zero-gap-class-unregistered-scripts.sh dropped out); podman nil-health failure UNCONFIRMED on the real host (every running container has a healthcheck). CONTROLLER sweep-classes.tsv EDITS PENDING: live-vs-source population_why: replace 'a defined service that is not running is itself reported' with 'is reported COULD-NOT-INSPECT (rc 2, never a finding, never clean)'; build-if-missing population: 'every tracked basename start*.sh / restart*.sh at any depth in the umbrella and every checked-out submodule, plus any tracked *.sh/*.bash that builds behind an existence test; minus _tests/fixtures/zero-gap/, the third-party trees of helix-deps.yaml comment block, and .specify/'; missing-toolchain: append '; plus derived:<tool> for each tool a verify/prove/registered script probes with no catalogue row (high only when followed by exit 2/return 2/an undet call, else low), plus a docs-drift check of CLAUDE.md/CONTINUATION.md'; untracked-blind-window: 'every registered entry point (column 3 of a check or debt row of scripts/check-registry.tsv; pre-push gates only through scripts/pre-push-gates.sh) whose enumeration reads git ls-files (tracked only) without also reading untracked-not-ignored files'."
  - "Class fix round DONE (content-boundary-rows): proof 46/46 (was 26 pass/29 fail RED before), 16 mutants all caught, recall 6/6, live rc 2 'cache absent' byte-identical x3 (expected until the one-time T036 cache refresh; no live precision measurable yet). All five rev-cbrows load-bearing items fixed: non-default gate parameters refused (9 recorded params), skipped files (not_indexed, files_unscanned, newline-named — measured not_indexed=0 for the newline case, the gate splits the name and counts it nowhere, now CNI), forged cache checks (per-class counts, fleet cross-check against .gitmodules/helix-deps/umbrella), FIFO//dev/zero hangs (timeout + non-regular -> CNI), header now says 'detects staleness and accident, not forgery'. Refresh recipe for T036: run the gate with --root <cp -a copy>, set .root to the live root with jq, manifest check then proves the live tree equals the copy; T036 MUST state verbatim 'until the one-time cache refresh this class reports rc 2 (cache absent) in every sweep'. Runner file was mid-edit (syntax error at zero-gap-sweep.sh:1168, mtime 05:43) during one sweep attempt: transient, retry clean. CONTROLLER sweep-classes.tsv EDIT PENDING (content-boundary-rows population): 'the sentinel token gate:scripts/verify-content-boundary.sh (the gate result itself, so a result with zero surviving rows is an inspected clean item) plus one PUBLIC PATH CLASS per group of surviving rows of scripts/verify-content-boundary.sh --json at its default parameters — the public repository fleet path (the umbrella . adds no prefix) plus at most two leading directory segments of the file — each FINDING located at <path class>:<gate class>; on the swept tree means the rows come from the cached result .remember/logs/zero-gap/content-boundary-rows.{json,corpus} used only after its manifest is recomputed and proven equal to the tree now; the fleet is the cached one, cross-checked against .gitmodules, helix-deps.yaml and the umbrella .'. Independent re-review of this class: one scoped re-review after the wave (rev-cbrows-2)."
  - "Class fix round DONE (safety): guard-gaps 19/0, unsealed-evidence 25/0, private-in-public 34/0; 16 mutations all caught (two first survived, new clean controls kill them); recall 1.0 each (unsealed-evidence sweep run UNSTABLE only because a concurrent implementer edited unproven-checks). private-in-public: placeholders @@ZG-AKIA/PRIVKEY-HEADER/ENTROPY/URLPASS@@ expanded at scan time in a $TMPDIR copy (no secret shape at rest), false critical on YAML rulings gone (0 content-boundary live), identifier-shaped tokens cleared, symlink/FIFO/newline-name handled. Re-measured: live 3 findings 1 real (1/3), 0 security 0 content-boundary; specs/002-009 sample: security 1/1 real (a literal password at specs/007-decouple-modules-auth/spec.md:184 — REAL DEFECT for T038, third-party spec text), content-boundary 0 (was 55), guessing 2/10 real; bare `appears`/`could be` stay out of the vocabulary (67 extra hits, all 12 read were factual/modal) — header records it. unsealed-evidence: 68 findings (8 high, 60 medium), precision 68/68 (git grep: no runner calls `record`); also fixed corpus-mode evidence_refs being directories (runner recall was UNKNOWN). guard-gaps: 10 sourced rows added (poweroff, pkill -f, killall, rm -rf /, filter-branch, branch -D, update-ref -d, clean -fdx, checkout ., add .), CANDIDATES hand list replaced by a `candidate` ERE column, timeout -k, clean `allowed` row; live 21 rows, 42 items, 16 high findings (guard exits 0 on every row except the 5 push/sudo/host-power rows) — the progress.yml real-defects list 'guard lets 6 through' is superseded: 16 live guard gaps for T038 (third-party upstream guard, REPORT not patch). Deferred: the UE grep rc-2 branch has no planted trigger (comparison file internal). Write tool refused a subagent report file; report was returned in the hand-back. CONTROLLER EDITS PENDING: (1) private-in-public population: 'every public record this feature produces: docs/zero-gap/** (incl. the register export), specs/010-zero-gap-verified-closure/** and the zero-gap entries of CONTINUATION.md, tracked plus untracked-not-ignored; the evidence streams are git-ignored under .remember/logs/zero-gap/evidence/ and belong to the FR-020 scanner (T062)'; (2) guard-gaps population: 'every row of docs/zero-gap/guarded-commands.tsv (command, source, and the dangerous command class it stands for in the candidate column), piped as probe DATA to the wired PreToolUse guard; tokens guarded:<id> and candidate:<id>'; (3) docs/zero-gap/README.md class contract: evidence_ref 'an existing regular file' (runner refok accepts only regular files) — coordinate with the runner round 3 owner, who owns that file."
  - "Class fix round DONE (gates): vacuous-gates 16/0 (11 mutations killed), unproven-checks 21/0 (14 killed), unregistered-scripts 18 (7 killed), coverage-gaps 38/0 (3 killed); corpus recall exact (10/10, 18/18, 5/5, 10/10), 3 live runs byte-identical, emit-population sha equals run sha; runner-level `--class` recall NOT run (runner file mid-edit, syntax error at zero-gap-sweep.sh:1231 at that moment). Shared exclusion applied as ONE `zg_thirdparty` block byte-identical in unproven-checks and unregistered-scripts (each proof asserts the copies match): helix-deps.yaml third-party comment block + .specify/extensions mirrors, config/covenant_propagation_exclusions.tsv vendored-third-party/generated-code rows, .specify/scripts/bash everywhere; unproven-checks skips the governance source (gitlink carrying scripts/gates/lib/pointer_carrier.sh, same test as cascade C1). Live re-measured: vacuous-gates 3 findings (was 19), 2/3 real — REAL: scripts/test-setup-agents-wizard.sh:1543 (skippable battery), scripts/audit/zero_findings_sweep.sh:525 (missing scanroot skipped by [ -d ] || continue); FP scripts/zero-gap-class-doc-count-drift.sh:516 (fixture-building loop); the earlier real-defects entries verify-workable-items.sh:278, continuation-check.sh:207, verify-content-boundary-judgement.sh:411, zero-gap-evidence-chain.sh:768, zero-gap-sweep.sh:884 are DROPPED (fixed-case proofs; zero cases impossible). unproven-checks 103 findings over 836 of 837 items, 10/10 hand-read true as stated (whether each *_challenge.sh really is a gate: unmeasured; the one non-inspected item is tracked symlink submodules/qa/scripts/anti-bluff-scan.sh); unregistered-scripts 16 findings, 8/8 true (match R5; the 6 .specify/scripts/bash files now excluded); coverage-gaps rc 2 (coverage.tsv absent, correct). Deferred: single-source the 12 required kinds with T052 (no generator yet). CONTROLLER sweep-classes.tsv EDITS PENDING: coverage-gaps population 'every cell of docs/zero-gap/coverage.tsv (subject x test kind): for every subject it names, the twelve required kinds of spec.md User Story 3 plus every extra kind listed, as cell:<subject>:<kind>; gap (incl. gap:<note>), a check cell whose ref resolves to no registered check id or tracked file, n/a without a reason, and a missing required row are findings; could-not-run is could-not-inspect'; unregistered-scripts population 'every *.sh tracked in the UMBRELLA (git ls-files, every depth, including below the maxdepth-1 boundary of the declared scanroots), minus _tests/fixtures/zero-gap/ and the shared third-party classification; owned submodules are NOT enumerated (none carries a registry, ~1,900 tracked *.sh), unproven-checks covers them'; unproven-checks: replace '(umbrella and owned submodules)' with '(umbrella and every gitlink that is neither third-party per the shared classification nor the governance source supplying scripts/gates/lib/pointer_carrier.sh)'; vacuous-gates: append '; a pre-push gate that runs no single shell file (go test, Playwright) is the item scripts/pre-push-gates.sh:gate_N and is could-not-inspect'."
  - "Class fix round DONE (docs): stale-figures 31/0, doc-count-drift 30 ok, pointer-drift 16/0; corpus recall 15/15, 14/14, 19/19; runner `--class` recall=1.0 with inspected==population (1540, 1518, 132), no CNI/UNSTABLE; 3 live runs byte-identical; RED seen on each (12 pass/22 fail, 28 fail, 12 pass/21 fail) with evidence re-captured into /tmp/zg-impl-E/<id>.md. stale-figures: docs/** scan dropped (belongs to doc-count-drift), hex ids not figures, `bytes` and exit/rc N shapes added (ruling 2), LOW = one aggregate per document `figures:<doc>`, symlink/FIFO//dev/zero -> CNI with ledger gate under timeout -k 5 300; live 1 high, 514 medium, 2 low aggregates (was 2676 findings), 7/10 medium hand-read real (FPs: a withdrawn figure whose marker is on the next line; two exit-code MEANING lines — deferred). doc-count-drift: umbrella carriers/README/CONTINUATION dropped, lockstep copies one finding with `also in`, NEW tracked docs/zero-gap/point-in-time.tsv (21 rows, reason from each doc header — MUST be in the wave commit), claim-key=<12 hex> stable under line insertion, cost no longer quadratic (240 KB 0.24 s, 1 MB 0.65 s), OOM on /dev/zero symlink fixed (old scanner was OOM-killed twice during the review/RED runs); live 3 high / 9 medium / 221 low aggregates (was 16/27/1484), 7 of 12 drift findings are stale counts today, 5 are dated measurements in specs/008-unified-workshop-platform/tasks.md task notes (file cannot be exempted whole), 10/10 aggregates real. pointer-drift: proof checks severity/category/direction on 19 planted cases, 6 branches covered, carrier findings collapsed 16->4, GIT_ALLOW_PROTOCOL=ssh:https:file set (ext:: remote ran a command when user config allowed it — C13), evidence_ref cites .gitmodules; live 23 medium (was 35); constitution's 8 remotes still give 8 findings (not requested). Not done: stale-figures gate timeout untested (+300 s), one equivalent mutant unkillable. CONTROLLER EDITS PENDING: stale-figures population 'every recorded figure (a count, a PASS/FAIL/CURRENT/DRIFT split, N of M, N/M, a byte/line/anchor count, an exit/rc N; a hex id is not a figure) stated in the four root carriers, CONTINUATION.md and README.md, every line a docs/claim-ledger.tsv anchor matches, plus every row of docs/claim-ledger.tsv; docs/**/*.md belongs to doc-count-drift; LOW unledgered figures are one aggregate per document (figures:<doc>)'; doc-count-drift population 'every numeric count claim found by scanning tracked Markdown — the umbrella docs/**/*.md and specs/**/*.md, and each checked-out owned module README.md CLAUDE.md AGENTS.md QWEN.md GEMINI.md docs/**/*.md specs/**/*.md (the umbrella carriers, README.md and CONTINUATION.md belong to stale-figures) — of the shape <number> <unit> (unit in files, tests, test files, gates, verify scripts, prove scripts, scripts, commits, items, rows, checks, services, submodules, anchors, lines, specs), <number> `<glob>` <word>, or <n> verify / <m> prove, MINUS the claims re-measured by a row of docs/claim-ledger.tsv; documents listed in docs/zero-gap/point-in-time.tsv are enumerated but never reported'; pointer-drift row unchanged."
  - "Runner round 3 DONE (resumed after the rate-limit cut): battery RED 117/11 on the round-2 runner -> GREEN 128/0 in a quiet mirror byte-identical to the live runner and fixture; 5x determinism sha 34d9988685600a78. I-A catalogue-schema asserts removed; I-B GIT_CONFIG_COUNT=2 pins core.excludesFile/attributesFile to /dev/null (Q1b mutation caught); I-C @git:hooks and @git:info digests in the fingerprint (Q3) and a rewritten ignored-path detector (Q2) — DEVIATION ruled acceptable: the prescribed find -cnewer + entry-list diff regressed the excluded-path cases (git lists no entry for an ignored tree holding no file so the scanned set differed before/after); shipped detector lists every ignored FILE (--untracked-files=all, 4969 files in 0.09 s), drops excluded paths, stats both sides, catches create/remove/write/cp -p; U+061C in BADSEQ (Q4); evidence_ref must be a regular file with realpath in --root or corpus, not under .git/, not fixture (Q5 caught 4); SIGSTOP-rescan-KILL for spawners (Q6b); SHELLOPTS=noexec documented (consumers must require the `VERDICT HOLDS rc=0` line); contract nit fixed; README rev 4. Residuals: escapee that drops ZG_RUN and leaves its group (cgroup scope via systemd-run --user --scope is available, rc 0, but adopting it is a design change — deferred, operator-visible), BASH_ENV/ENV/SHELLOPTS=noexec reach the first bash, empty ignored directories unseen. STATE: ALL 17 classes implemented, reviewed and fixed; remaining Phase 3 work is controller-serial: sweep-classes.tsv row edits, ledger corrections, tick T017-T034/T081, CONTINUATION entry, registry check, wave commit, then T035/T036/T037/T038."
  - "Phase 3 wave committed and pushed: umbrella 39500ed013e8 on github/origin/upstream (all pre-push gates passed). T035 determinism harness (scripts/zero-gap-determinism.sh, 33/33 proof, 26 mutations caught; real full sweep x2 IDENTICAL rc 0, 1043 findings, 577 s; aligned to tasks.md: default N=5, --check <class_id>). Independent review rev-t035: ACCEPT-AFTER-FIXES. Fixes ordered: (1) CRITICAL no free-space/tmpfs check before the ~14 GB copy (default scratch /tmp is RAM-backed, 5.8 GB free) — estimate size (du -sb of listed paths + .git), rc 2 if df available < 1.2x, rc 2 if findmnt FSTYPE is tmpfs/ramfs unless --allow-tmpfs; (2) SIGINT/SIGTERM/HUP: start the runner with &, record pid, wait, trap and TERM the recorded timeout pid, clean up, exit 128+signal; proof cases send TERM and INT mid-run and assert no process left + empty scratch; (3) default run copies ignored files incl. .env of untracked nested repositories (header claim 'ignored files NOT copied' false for that case) — copy nested repos through their own ls-files -c -o --exclude-standard list plus their .git; (4) --include follows symlinks out of the tree — strip trailing slashes, refuse when any component is a symlink or realpath -e is not under --root; (5) proof survivors: run-2-only comparison (add a case differing only on run >=3 at default N=5 with state outside the copy) and M10f (assert no zg-determinism.* entry ever created under root and reason text 'lies inside --root'); minors: directory-mtime-only change reports DIFFERS not UNDETERMINED (document as stated limit or include dir mtimes outside .git), cite zero-gap-sweep.sh:47 for the byte-identical phrase, date the '~14 GB / ~6 GB free' measurement, orphan-on-signal in stated limits until fix 2 lands. Measured by reviewer: --check unproven-checks --runs 2 IDENTICAL rc 0, copy 83 s, runs 56 s each, peak disk 14.8 GB, scratch empty after."
  - "T035 fix round DONE: --prove-failure 48/48 (9 controls, 39 mutations caught), one real full run --runs 2 on /var/tmp: estimate 14.75 GB vs 1.0 TB free, copy 84 s, runs 232/229 s IDENTICAL rc 0, scratch cleaned. Fixes RED->GREEN: tmpfs/ramfs refusal + /var/tmp fallback + 1.2x free-space check; INT/TERM/HUP trap terminates the recorded runner, removes scratch, exits 128+signal; nested repos copied via their own ls-files list (ignored .env no longer copied); --include refuses symlink components / paths outside root; D5 (run>=3-only difference) and stricter M10f. Two mutants survive by construction (include realpath-prefix backstop unreachable because .. and every symlink component are refused first; trailing-slash strip redundant because read -a drops the empty component) — accepted. Residual: class processes the runner put in their own session (setsid) are not reached by the forwarded TERM. Ruling: independent re-review of the fix round is SCOPED to fixes 1-4 and runs after the T036 baseline while the harness is in use (rev-t035-2); the harness is committed now because every critical fix has an observed RED and the original independent review accepted the core."
  - "T036 BASELINE DONE_WITH_CONCERNS (baseline files in specs/010-zero-gap-verified-closure/baseline/sweep-2026-09-26/: README.md, report.txt, report.json, recall-table.md, FINDINGS-BY-CLASS.tsv). Swept state: one frozen real copy of umbrella 50470448d594 (rsync -a, no hardlinks), fingerprint 9685cc2b (files=8407) identical before/after the copy and every run. VERDICT VIOLATED rc=1 findings=1229 (221 high, 750 medium, 258 low) could-not-inspect=15 classes=17 unknown-recall=0, 267 s; every class recall 1.0 on its corpus, zero RECALL-* findings; runs 1 and 2 byte-identical (sha256 c67a9f5f…), a third --json run found the same 1229-finding set. Per class (high/medium/low, CNI): build-if-missing 1/4/2 CNI1; content-boundary-rows 186/0/0 CNI2; coverage-gaps 0/0/0 CNI2 (coverage.tsv absent); doc-count-drift 3/9/222; guard-gaps 16/0/0; improvement-candidates 0/2/28; known-open-decisions 0/3/0; live-vs-source 0/0/0 CNI5; missing-toolchain 3/3/4; pointer-drift 0/23/0; private-in-public 0/3/0; stale-figures 1/519/2; unproven-checks 0/103/0 CNI2; unregistered-scripts 0/16/0; unsealed-evidence 8/61/0; untracked-blind-window 0/4/0; vacuous-gates 3/0/0 CNI3. Content-boundary cache refresh: the gate ran in the copy, rc 1, 2030 s, default parameters, stable corpus, 14621 surviving rows, stored cache has 0 .match keys; so this sweep read the cache (186 high path-class findings) and did NOT report 'cache absent'. The ruling sentence stays on record verbatim: until the one-time cache refresh this class reports rc 2 (cache absent) in every sweep. A copy with .root set to the live root sits in the git-ignored .remember/logs/zero-gap/ and goes stale as soon as any fleet file changes. DEFECT (recorded, not fixed; feeds a T035/class fix): scripts/zero-gap-determinism.sh --include (det_rsync_copy + include step) and the copy recipe in the content-boundary-rows class header lose an ignore rule that lives in an ignored file — .remember/.gitignore holds `*` and is not copied, so the copy counted 8409 files not 8407 and the gate treated its own output as unscanned untracked files (the first 2057 s gate run was discarded). Residuals: live-vs-source is blind on a copy (its 5 CNI are artefacts: compose label names the live root, qa-up state files ignored — needs a live-tree run); recall is corpus-only (5-19 planted rows per class) and precision of stale-figures/doc-count-drift was not re-measured; 11 findings sit inside private submodules (paths and counts only, someone must re-read them); the word likely appears once in the new files as the term a private-in-public finding flags. DB unchanged (9413fd96f2647c63). Recall column of sweep-classes.tsv NOT edited (publishing is T038)."
  - "T038 baseline review rev-base-B (9 classes) — consistency PASS (TSV/report.txt/report.json equal row for row; 1229 = 221/750/258; CNI 15; 186 content-boundary rows sum to 14621 = cache counts.leaks); private-text audit CLEAN (AKIA/PEM/tokens/.match/home/email all 0; all 11 findings inside private submodules and 124 more naming a private path carry only paths, counts, fixed units); live re-run reproduces unsealed-evidence, untracked-blind-window, doc-count-drift, known-open-decisions, improvement-candidates exactly; content-boundary-rows now rc 2 'cache stale: 2 enumerated file(s) differ' (expected: the cache is point-in-time); live-vs-source live = 3 findings (1 high: qa-up/workshop serves 185fb15 while HEAD is e076fa6; 2 medium: compose container started before HEAD, ai stamp not tied to its source). Precision: unsealed-evidence 8/8 H + 8/8 S, doc-count-drift 3/3 H + 6/6 S, untracked-blind-window 4/4, content-boundary-rows 6/6, improvement-candidates 6/6, private-in-public 1/3 (progress.yml:72 'possibly' as vocabulary, :43 'should be' normative), known-open-decisions 1/3 (:68 and :77 review-log lines, :77 already records :68 as FP). CRITICAL C1: baseline report.json self-pollutes the next sweep — its files sit inside private-in-public's own population and JSON string quotes around private paths look like quotation: live private-in-public gives 130 findings (126 from the baseline dir: 123 CRITICAL private-path-cited-with-quotation at report.json lines + 3 'should be' at report.txt:465, TSV:466, report.json:468) instead of 3. RULING: do NOT commit report.json (TSV is row-identical; record its sha256 in the README) and record the 3 guessing-term self-reference rows in Honest limits; ALSO ledger a private-in-public DETECTOR defect for the closure wave: JSON string delimiters must not count as a quotation (any future JSON that names a private path would trip it) — RED case + fix. Important: I1 README must quote the ruling exactly as CONTINUATION.md:489 reads ('content-boundary-rows reports rc 2 (cache absent) in every sweep until the ONE-TIME cache refresh'); I2 state 135 findings name a private path (11 located inside, 117 low figures: rows, 7 more in descriptions), all paths/counts/units, measured; I3 replace 'needs a separate live-tree run' with the live result (3 findings above); I4 known-open-decisions caveat: 2 of 3 known FPs. Minors: 'quotes the guessing term' -> 'all three'; README Recall column needs N; reproduce recipe needs `--root <absolute path>`. Honest recall wording (binding for publishing): 'recall 1.0 on the class's planted corpus (N = <planted> rows; 5-19, 195 total); a floor for that corpus only; recall on the live population is not measured'; for content-boundary-rows add 'synthetic fleet, N = 6; not a recall of the gate'. Verdicts: ACCEPT-AS-BASELINE unsealed-evidence, untracked-blind-window, doc-count-drift (lows informational), improvement-candidates; ACCEPT-WITH-CAVEAT content-boundary-rows ('point-in-time from copy 50470448; live cache stale since; 186 unjudged'), coverage-gaps ('not inspected until T052; coverage.tsv absent'), private-in-public ('precision 1/3; baseline dir self-pollutes'), known-open-decisions ('precision 1/3'), live-vs-source ('baseline equals not inspected; register the 3 live findings separately'). Waiting on rev-base-A (8 classes) and T037."
  - "T038 baseline review rev-base-A (8 classes) — consistency PASS (TSV/report.txt/report.json cmp-identical; per-class severity recount matches README and recall-table; key is not unique: doc-count-drift has 2 duplicate keys specs/008…/tasks.md:3607/:3608 => README must say '1229 rows, 1227 distinct keys'). Precision: stale-figures 6/6 (incl. the only high: README badge 23 vs zero_findings_sweep.sh 39), vacuous-gates 2/3 (FP: zero-gap-class-doc-count-drift.sh:687 mkrepo setup loop inside a proof helper; zero_findings_sweep.sh:525 real; test-setup-agents-wizard.sh:1543 weak — claim literally true, cases inline), unproven-checks 7/7, pointer-drift 7/7 (confirmed with ls-remote/is-ancestor for LLMProvider, llms_verifier, constitution; findings go stale with the remotes; minor: qa pushurl2/pushurl3 same URL give two findings), build-if-missing 7/7 (FN: scripts/verify-workable-items.sh:185 same pattern with the build in the `if` condition `[ ! -x ] && ! build_tool` missed), unregistered-scripts 6/6 (declared blind spot: non-.sh executables e.g. _tests/preflight.js, _tools/translate/glossary_protect.py, _tests/evidence/fix4docs/glossary_protect_v2.py), guard-gaps 16/16 (re-probed: exactly the 16 rc-0 rows; population bounded by the catalogue — reboot, halt, git push origin :main, git push --delete origin main, rm -rf ~ also exit 0 and are uncatalogued; stash-all cites only the feature's own tasks.md). missing-toolchain: REJECT — CRITICAL all 3 highs are FPs (maybetool, newghost, zgselftest are the class's own proof/self-test strings at zero-gap-class-missing-toolchain.sh:151/159/322: the class scans its own source); CRITICAL all 3 mediums (bundle, jekyll, tesseract) contradict operator decision #10 of 2026-09-07 ('CONTAINERISED … not host packages') and #4 of 2026-09-09 (validate-pdf.js resolves a container OCR engine, preflight.js names the container site-build) so 'gate exits 2 until installed' and the apt-get/gem remedies are false and the toolchains.tsv rows are stale; IMPORTANT results depend on PATH (the README recipe env -i PATH=/usr/local/bin:/usr/bin:/bin adds high derived:node and low derived:glab; node lives under ~/.nvm, glab under /snap/bin). Fix: exclude the class's own script, update toolchains.tsv to the container remedies, pin or record the PATH, re-baseline. stale-figures caveat: the 519 medium/2 low mean 'figure has no re-measure command', NOT 'stale'; staleness only for figures with a ledger row; FN CLAUDE.md:315 says '14 declared' but .gitmodules declares 22 (class reports the line only as unanchored '0 DRIFT'); register wording 'unanchored recorded figures (staleness unmeasured)'. RECALL WORDING (supersedes the earlier one; 'a floor' is the wrong word): 'Planted-corpus detection: 1.0 (N=5-19 per class), a regression check of known patterns; live-population recall UNMEASURED.' The self-scan FPs show corpora do not test precision on the class's own sources. README fixes: item 4 (missing-toolchain highs not real), item 5 points to this review, honest limits gain 'missing-toolchain depends on PATH'. RULING (baseline composition): fix round F1 (missing-toolchain + toolchains.tsv) and F2 (private-in-public JSON-delimiter detector defect RED->GREEN; vacuous-gates FP on proof-helper setup loops; build-if-missing FN at the `if`-condition build; guard-gaps sourced rows for reboot, halt, git push origin :main, git push --delete origin main, rm -rf ~); then RE-BASELINE ONLY THE CHANGED CLASSES on a fresh frozen copy through the runner --class (each class is deterministic), and publish the baseline as PER-CLASS snapshots with the umbrella commit each was measured at (content-boundary-rows stays from copy 50470448 with its cache); the recall table gets those commits. Then the T038 human checkpoint."
  - "T037 DONE (impl-t037): `workable-items … report --by-module [--as-of]` on the existing report subcommand (main.go), logic in _tools/workable-items/internal/wi/modulereport.go; copies the register to a private temp dir with sha256 before/after and opens ONLY the copy mode=ro&immutable=1, reuses the roster loader and the V-G1..V-G12 validator, own check MODULE (an item whose id prefix is not in the roster => 'Unplaced items', counted as a finding). Exit codes proven in tests: rc 0 golden-good page byte-identical to module-page-golden-good.md (twice, different dirs, no temp paths/clock); rc 1 golden-bad names MODULE ZZZ-001, V-G1 VSC-005 (kind+category missing), V-G3 VSC-004 (no verifier); rc 2 with reason and NO page for absent/directory/unreadable/non-SQLite/unmigrated/partially migrated/non-empty -wal/bad --as-of/unreadable roster. RED against stub 6/7 (7th NeverWritesTheRegister verified by mutation to writable Open). go build/vet ok; go test ./... 299 results: 298 PASS + 1 FAIL = the pre-existing TestRosterLoadsAndCrossChecksTheLiveFleet (8 submodules lack a roster identifier). Live DB sha unchanged 9413fd96f2647c63; `report --by-module` on it exits 2 'no zero-gap schema' and prints no page. Walk-through doc docs/zero-gap/module-page-walkthrough.md on the golden-good fixture (live register unmigrated, stated): page 207 lines / 9,403 bytes, 15 module sections, render 35 ms, VSC's 3 open items found in 42 ms, exits 1 because V-G8 names the 8 roster gaps T047 closes; the 2-minute SC-010 target is script timing only — whether a PERSON reads a section in under 2 minutes is PENDING an operator walk-through (SC-010 not claimed met). Files new: modulereport.go, modulereport_test.go, registers/golden-bad/module-page.sql, registers/module-page-golden-good.md, docs/zero-gap/module-page-walkthrough.md; modified: main.go, main_test.go. Independent review pending (rev-t037)."
  - "T037 review rev-t037 (opus): ACCEPT-AFTER-FIXES. CRITICAL C1: the page says 'no finding' when validate finds one — it runs only V-G rules plus its own MODULE check and skips the base validator's rules (deleting one item_provenance row: page rc 0, validate rc 1 provenance/missing; on golden-bad the page reports 13 findings, validate 14) and MODULE re-implements id/prefix-not-in-roster contradicting the header — fix: run the FULL validate rule set and render all its findings, drop MODULE and place by id/prefix-not-in-roster, test page findings == validate findings on golden-bad and the provenance-missing register. CRITICAL C2: a symlinked --db skips the -wal check (stat src+'-wal' but SQLite keeps the WAL beside the resolved file; real path rc 2, symlink rc 0 'no finding'; a hot -journal not checked) — EvalSymlinks first, refuse non-empty -wal or any -journal beside the resolved file, tests for each. IMPORTANT I1: item id printed raw can spoof module headings (newlines in atm_id produced 7 fake `## WSP` headings above the real VSC section, breaking the walk-through grep) — render every id through clean()/code() (or %q), test. I2: 4 of 9 mutations survive (M4b undetermined validator rows rc 0 with page printed; M6 sha double-read comparison untested; M2b module prefixes unsorted — fixture roster already sorted; M2d findings unsorted) — add tests and an unsorted roster fixture. I3 RULING: T037 UNTICKED and annotated in tasks.md — plan.md:177 asks a timed MANUAL walk-through and the recorded timing is a script's; ticked only when the human walk-through is recorded at the T038 checkpoint (SC-010 not claimed; grep confirmed nothing in the repo claims it). Minors: stdout.Write errors ignored (rc 2 on write error), no SIGINT/SIGTERM handling (temp copy left in /tmp), walk-through doc wrong about rc 2 => no page (undetermined validator rows print the page) and lacks 'register changed while being read', contracts/register-cli.md does not document report --by-module (add), legacy counts for prefixes outside the roster silently dropped, titles not truncated / not HTML-escaped (<script> passes raw), a roster path containing | splits its table cell. Measured no issue: 10k items 0.72 s / 54 MB RSS, 10k items + ~2k findings 0.79 s, deterministic (severity then id), duplicate roster prefix rc 2, live path read only via plain file reads, copy opened ro&immutable."
  - "Fix round F1 DONE (missing-toolchain): --prove-failure 30/0 (9 own-fix mutations all caught), runner --class recall 1.0, 3 live runs identical (stdout+stderr). (1) self-scan: every scripts/zero-gap-class-*.sh excluded by name (P19 live: no maybetool/newghost/zgselftest; P20 by-name only); (2) container route: catalogue gains a sixth column container_route (`none` or `src= bin= build= runtime=`), a finding needs host tool absent AND route none/unusable (P21a-f); bundle/jekyll/tesseract rows now name the containerised remedy and cite both decision docs by path; (3) PATH: no gate searches ~/.nvm or /snap/bin so the class does not add them; it records the probe PATH on the first stderr line (sha256, HOME shown as ~) and each host-dependent finding carries that sha (P22). Live: 30 items hand-read 6/6 correct; under env -i PATH=/usr/local/bin:/usr/bin:/bin: high derived:node (self-validate.sh:171 exits 2 without node) + low derived:docs_chain/glab/ifconfig/lumen/shellcheck (optional/fallback checks); under the interactive PATH the 4 lows only; bundle/jekyll/tesseract NOT reported (route binaries built, podman present, both images local) — end-to-end run of those workloads UNCONFIRMED. sweep-classes.tsv missing-toolchain row updated by the controller. README honest limit: 'missing-toolchain depends on the caller's PATH; the PATH is recorded (first stderr line, and its sha in each finding)'."
  - "T037 fix round DONE: all rev-t037 fixes applied test-first, 14/14 mutations killed by failing tests (M4b, M6, M2b, M2d, base rules dropped, wal check on given path only, journal check removed, raw id in heading, title not escaped, title not truncated, write error ignored, pipe not escaped, legacy-outside count dropped, signal cleanup skipped). C1: full base+V-G1..V-G12 rule set on a register copy prepared the way `validate` prepares one, private MODULE rule removed, id/prefix-not-in-roster + id/malformed go under 'Unplaced items', FindingSetEqualsValidate test + a CLI comparison of report --by-module stderr vs validate stdout on golden-bad and a provenance-missing register (match). C2: --db resolved through symlinks, non-empty -wal or any -journal beside the resolved file or given path => rc 2 (wal-with-frames, hot journal, empty journal each via real path and symlink). I1: ids/text one line, HTML-escaped, titles cut at 200 chars (stated on the page), exactly one `## VSC — ` and one `## WSP — ` heading under a spoof test. Minors: stdout write error rc 2, SIGINT/SIGTERM remove the temp copy (exit 2), legacy items outside the roster counted, `|` escaped, walk-through doc corrected, contracts/register-cli.md documents report --by-module. go test ./... 318 results = 317 PASS + 1 FAIL (pre-existing TestRosterLoadsAndCrossChecksTheLiveFleet); vet/build ok; registry rc 0; hardcoded-paths rc 0; live DB sha 9413fd96f2647c63; walk-through re-measured 211 lines / 9,901 bytes, one VSC heading, render 26 ms, lookup 33 ms, exits 1 (8 V-G8 roster gaps + one could-not-determine row from the base rules for the same 8 submodules). T037 stays UNTICKED (human walk-through pending). Scoped re-review of the fix round (C1/C2/I1) is folded into the T038 checkpoint pack."
  - "Fix round F2 DONE (private-in-public, vacuous-gates, build-if-missing, guard-gaps): proofs 36/0, 16/0, 30/0, 23/0; mutations on mirror copies all killed (private-in-public 4: JSON flag off/json_quote returning 0/curly-quote decoding removed/unescaping removed; vacuous-gates 9; build-if-missing: condition check off, ! strip removed, 3 connector variants, then-cut; guard-gaps: stash source reverted, row deleted, halt flipped to allowed, ROOT/CORPUS path resolution removed). (a) json_quote() for *.json/*.jsonl/*.ndjson: JSON string delimiters are syntax, each decoded value checked for a quotation inside it (escaped quote, curly quotes) — distinguishable, planted report.json:4/5/6 still found; live 130 -> 7 (all 123 CRITICALs from the baseline report.json gone; 1 real sweep-classes.tsv:21; 6 not: progress.yml:43 normative, :72/:86 vocabulary mentions, 3 baseline rows repeating :43). (b) vacuous-gates: a loop inside a proof harness is setup, not a verdict, when its only failure lines are test-free `cmd || return 1`; five planted judging-in-a-helper shapes still reported; live 3 -> 2 (zero_findings_sweep.sh:525 real; test-setup-agents-wizard.sh:1543 true by the rule but weak). (c) build-if-missing: if-condition arm (right connector only) and leading `!` stripped; live 7 -> 8, new hit scripts/verify-workable-items.sh:185 (medium) real — its proof path runs a stale workable-items binary; 8/8 real. (d) guard-gaps: 26 rows / 52 items / 21 findings (was 42 items / 16); proof L2 (every row cites an existing document other than this feature's tasks.md) and L3 (the 5 new commands catalogued as refused); stash-all and 4 other shared-tree rows now sourced to .specify/memory/constitution.md 'Shared-Tree Discipline Is Wider Than stash'; guard exits 0 on reboot, halt, git push origin :main, git push --delete origin main, rm -rf ~ (piped as data, nothing executed); EXTRA DEFECT fixed: a relative --root made every probe exit 127 so the class reported CNI instead of verdicts (R1/R2). No sweep-classes.tsv wording change. NEXT: re-baseline only the changed classes (missing-toolchain, private-in-public, vacuous-gates, build-if-missing, guard-gaps) on a fresh frozen copy, apply the README/recall-table corrections from rev-base-A/B, drop report.json from the committed baseline (record its sha256), publish per-class snapshot commits; in parallel one scoped independent review (rev-f) of F1/F2, T035 fixes 1-4 and the T037 fix round C1/C2/I1."
  - "Scoped review rev-f of f05817ba3c7a: F1 missing-toolchain ACCEPT-AFTER-FIXES; F2 ACCEPT-AFTER-FIXES; T035 fixes 1-4 ACCEPT (minors); T037 C1/C2/I1 ACCEPT (go test 317 PASS + the 1 pre-existing FAIL; golden-good byte-identical; provenance-row-deleted page finds == validate (1=1, golden-bad 6=6); real uncheckpointed WAL 4152 bytes rc 2 via real path, symlink, 2-hop chain, hot journal; newline spoofing across 21 text columns + verdict actor/evidence_path fields => exactly 1 `## WSP` heading, 0 injected `### Open`). FIX ROUND F3 (apply AFTER the re-baseline agent finishes, to avoid disturbing its live-tree runs), test-first: F1 IMPORTANT container route FOLLOWS a symlinked `tools/bin` directory pointing outside the tree (rc 0 'binary built') and a symlinked resolver binary check survives mutation — in route_state refuse (rc 2) when ANY component of src= or bin= is a symlink (as determinism's acc loop does), proof cases for a symlinked binary and a symlinked directory component; minor: state in WHAT IT DOES NOT SEE that excluding every zero-gap-class-*.sh also hides those scripts' real preconditions. F2 vacuous-gates IMPORTANT two false negatives: a loop in approve_release() running `gpg --verify … || return 1` is NOT reported because 'prov' matches as a substring (identical body in check_all() IS reported) and prove_syntax() running `bash -n \"$f\" || return 1` is exempt — match the name as (^|_)(prove|proof)(_|$) and treat a chain as setup ONLY when all its commands are fixture builders (git init/add/commit, cp, mkdir, ln, printf/cat redirect) instead of excluding a short list of judging commands; planted cases for both shapes plus clean cases for `prove_x() {` and `|| exit 1`; survivors to kill: `{`-function harness exemption off and `|| exit 1` refused as setup. private-in-public: survivors reading an unterminated string and the `\"` mapping — add cases; minors document (single-quoted quotations never count — older behaviour; pretty-printed JSON puts location and description on separate lines and escapes the line-level rule). build-if-missing: survivor 'condition truncated to 8 chars' (planted builds all short) — add a long-condition case; minor `[ ! -x ] || ! make -q` freshness guard still reported (older). guard-gaps minors: source cells must say 'class named; bare command inferred' where the source only names the class (the §11.4.109 row names `systemctl …reboot/halt`; §9:354 names 'branch deletion, bulk file removal'; CONST-033 appears only in docs/AGENT_GUARDRAILS.md, not in the canon Constitution.md); 3 of 8 benign commands match candidate EREs (echo will reboot later, grep -n halt build.log, rm -rf ~/Projects/x/build) — no impact because candidates are only tested against catalogue rows: document. T035 minors: add a ramfs stub case and an in-root intermediate-symlink case (--include dir/inlink/sub); CORRECT the ledger wording: the symlink check on intermediate --include components is NOT proved (the earlier 'two survivors by construction' claim was too generous for that one)."
  - "Re-baseline DONE (f05817ba3c7a, frozen copy fingerprint 23a3b9ff files=8447 equal on source, copy and every run; nothing seeded; DB sha unchanged): composed baseline 1239 rows, 1237 distinct keys; 223 high / 755 medium / 261 low; 10 CNI (report.txt alone: VERDICT VIOLATED rc=1 findings=1050 over 16 classes). Per class (H/M/L, CNI): build-if-missing 1/5/2 CNI1; content-boundary-rows 186/0/0 CNI2 (snapshot from copy 50470448 — new copy reports 'cache stale: 59 files differ', gate not re-run, labelled point-in-time); coverage-gaps 0/0/0 CNI2; doc-count-drift 3/10/225; guard-gaps 21/0/0; improvement-candidates 0/2/28; known-open-decisions 0/3/0; live-vs-source (LIVE tree) 1/2/0 = qa-up/workshop serves 185fb15 vs HEAD e076fa6 (high), compose container started before HEAD (medium), ai stamp not tied to source (medium); missing-toolchain 0/0/4; pointer-drift 0/23/0; private-in-public 0/7/0 (not inflated: the 3 self-reference rows moved to the new files); stale-figures 1/519/2; unproven-checks 0/103/0 CNI2; unregistered-scripts 0/16/0; unsealed-evidence 8/61/0; untracked-blind-window 0/4/0; vacuous-gates 2/0/0 CNI3. Identity: runs 3 and 4 byte-identical (sha f92eab8a), --json same 1050 findings; runs 1 and 2 differed only in pointer-drift (constitution remotes moved 2d86614 -> 647f755 mid-session, one gitlab probe timed out — remote-dependent by design); the five changed classes identical in all four runs. Changes vs the first pass: 17 keys added, 7 removed (missing-toolchain -6, vacuous-gates -1, build-if-missing +1, guard-gaps +5, private-in-public +4, doc-count-drift +4 (new files), live-vs-source +3). Checks: TSV rows == report.txt == report.json == 1050 for f05817ba; no /home/; README/recall table free of guessing words and 'floor' (the only 'floor' is in the verbatim first-pass README under superseded-first-pass/); runner outputs hold 8 quoted flagged terms (likely x2, possibly x1, should be x5); registry rc 0; hardcoded-paths rc 0. RULINGS on the 5 deviations: (1) report.json kept in the committed baseline — ACCEPTED, the F2 JSON-quotation fix removed the pollution (live private-in-public 7); (2) TSV 7th column `measured_on` ACCEPTED; (3) two backing snapshot files content-boundary-rows-50470448.txt and live-vs-source-live-f05817ba.txt ACCEPTED; (4) planted N re-measured 5-22, total 210 (not 195) because F1/F2 grew the corpora — ACCEPTED, the binding recall wording uses measured values; (5) private-path figure 322 (128 located inside, 186 content-boundary descriptions, 8 others) is a WIDER definition than rev-base-B's 135 — ACCEPTED with both definitions stated in the README; all paths/counts/units. NEXT: F3 fix round (rev-f findings) then re-measure ONLY vacuous-gates, missing-toolchain, build-if-missing, private-in-public, guard-gaps directly on a quiet tree through the runner --class (deterministic, fingerprint-checked) and update those classes' rows in the baseline; then the T038 human checkpoint pack."
  - "Fix round F3 DONE (all rev-f findings): missing-toolchain 32/0 (P21g/P21h/P21d: any symlink component of src=/bin= => rc 2 CNI; symlinked tools/bin pointing outside the tree was rc 0 'binary built'); vacuous-gates 16/0 (whole-word harness name (^|_)(prove|proof)(_|$); a chain is setup only if EVERY command is git init/add/commit, cp, mkdir, ln, printf/cat into a file (not /dev/null); 22 planted rows; mutations killed: {-harness exemption off, || exit 1 refused as setup, substring name, redirect/devnull, split, continuation, inproof off); live 2 -> 2 unchanged (zero_findings_sweep.sh:525 real, test-setup-agents-wizard.sh:1543 weak); private-in-public 36/0 (truncated.jsonl:1/:2 kill the unterminated-string and quote-mapping survivors; keeping the backslash on \\\" survives but is equivalent for detection; header documents single-quoted spans and pretty-printed JSON); live 7 -> 8 because the tree moved (new progress.yml:95 is a vocabulary mention, not real; precision 1/8) — this class is self-referential to progress.yml, its live count moves whenever the ledger is edited; build-if-missing 31/0 (p15 long condition kills the 8-char truncation survivor; header documents `[ ! -x ] || ! make -q` still reported); live 8 -> 8; guard-gaps 24/0 (L4 RED on 11 rows: class-only sources §9 and the §11.4.109 host-power row now say 'class named; bare command inferred'; CONST-033 cites docs/AGENT_GUARDRAILS.md item 5; header documents that 3 of 8 benign commands match candidate patterns, no impact); live 21 -> 21. T035 harness F3: S1f (ramfs stub refused, names the fstype) and I4 (in-root intermediate symlink in --include refused, names the component) — mutants killed; quiet-tree `--prove-failure` rc 0: 50 passed / 0 failed in 51 s (9 controls, 41 mutations caught), live tree byte-identical before/after (fingerprint ca4eb4d42359d4d1). Registry rc 0, hardcoded-paths rc 0, DB sha 9413fd96f2647c63. RULING: the published baseline (1239 rows, f05817ba3c7a) STANDS for the five classes — their live results are unchanged by F3 (vacuous-gates 2, missing-toolchain unchanged, build-if-missing 8, guard-gaps 21) except private-in-public (self-referential; 7 in the baseline, 8 after the ledger grew) — no second re-baseline; the README's per-class 'measured on' column stays f05817ba and the F3 classes carry the note 'detector strengthened after the snapshot; live result unchanged'."
  - "Phase 4+ readiness map (read-only Explore agent; the harness flagged its output as instruction-shaped only because it mentions `.claude/settings.json` guard changes for T043 — a plain mention, not an instruction). GLOBAL BLOCKERS: T038 not run (no seeding, no human checkpoint, no gap freeze --cycle 1); DEADLOCK: contracts/register-cli.md:48-50 keeps the live DB unmigrated until T047 (migration flips verify-workable-items G5 rc 2 -> rc 1) but T038 seeds with `gap add`, which exits 2 on an unmigrated DB (register-cli.md:46) — OPERATOR DECISION needed at the checkpoint: migrate early (pull T047's roster rows + migration forward with the operator ratifying the 8 roster prefixes) or seed into a migrated COPY/proposal only; not yet existing: scripts/zero-gap-verify.sh, zero-gap-guard-checks.sh, zero-gap-coverage.sh, docs/zero-gap/CLOSURE.md, docs/zero-gap/anchor.json, ops/systemd/. Phase 4 (US2): T039 verify-item, T040 guard-checks + pre-push, T041 third-party edit guard (after T040: shared pre-push-gates.sh), T042 CLOSURE.md, T043-T049 waves (only one committer per repository; T045 stale figures and T047 carrier/doc counts both edit the lockstep carriers and claim-ledger => serial), T050 operator queue, T051 evidence-pair review; T039/T040/T042 parallel. Phase 5: T052 generator -> T053 -> T054-T060 parallel (one heavy job at a time) -> T061. Phase 6: T062 FR-020 scanner, T063 wire-digest rule (parallel), push-path serial chain T040 -> T041 -> T062 -> T065, T064 heavy (5x --run-proofs ~2.8 h, alone), T066, T067. Phase 7: T068 (needs the T014 N3 witness-fetch fix FIRST: --no-recurse-submodules --no-auto-maintenance -c remote.<r>.uploadpack=git-upload-pack, correct history.go:384 and header text) -> T069 -> T071; T070 parallel; T072 queues the timer; T073 after all waves. Phase 8: T074-T080. OPERATOR ACTIONS/DECISIONS: T038 checkpoint + improvement cap + SC-010 walk-through; T047 roster prefix ratification; T043 .claude/settings.json guard changes; T049 tesseract/webkit installs; T050 gitlink bumps + 3 behind-remote submodules; T072 timer install; T078 amendment approval; T080 pushes to site modules and every gitlink bump; the first write/commit of docs/zero-gap/anchor.json has NO owning task. RECOMMENDED FIRST WAVE: (1) fix N3, (2) roster rows + live migration (resolves the deadlock, operator ratifies prefixes), (3) T038 seed/checkpoint/freeze, (4) T039+T040+T042 parallel then T041, (5) T043 security, T044, T046 (disjoint files, highest severity), (6) T045 then T047 serially, (7) T048/T049 last. tasks.md INCONSISTENCIES (line numbers per the reviewer): [393] says US2 needs a seeded register '(T036)' but seeding is T038 [211], and [410] puts the checkpoint at T036 vs [213] T038; [396] says T065 comes 'after T013-T015' and is 'the only task touching the push path', contradicting [53]/[322] (after T041) and T062 [317], and omits T062 from the push-path order; [393] lists T039-T041 as wave prerequisites but omits T042 which every wave reads [226] (T040/T041 are not really wave prerequisites); no wave covers categories test-coverage or other (data-model.md:45-46) and none is named for kind=improvement; T032 ticked [189] but stays CNI until T052, and no task re-sweeps coverage-gaps after T052 although [306] requires every gap to be a register item (after the freeze such items can only go to cycle 2); no task creates/commits the first tracked docs/zero-gap/anchor.json (progress.yml:37) while T068 needs --verify rc 0 [343] and T074 needs the anchor [370]; the N3 fix [341] has no task id (T014 is ticked); [341] says 'T068 runs the witness fetch daily' but the daily job is T069; T051 and T073 overlap (both re-check all closed items). CONTROLLER TODO before Phase 4: add tasks for N3, anchor.json, the coverage re-sweep, test-coverage/other/improvement waves and fix the cross-reference lines (a tasks.md correction pass — does not need the operator)."
  - "Seed dry-run seed-gates DONE (nothing written to the register; DB sha 9413fd96f2647c63; proposal files under /tmp/zg-seed/gates/: proposed-items.tsv 24 items x 19 columns, members.tsv 215 rows, coverage.txt COVERED: 215 baseline rows = 215 assigned, 0 twice, 0 unassigned, 0 dropped; per class unproven-checks 103, guard-gaps 21, unregistered-scripts 16, unsealed-evidence 69, untracked-blind-window 4, vacuous-gates 2, coverage-gaps 0; the 7 CNI lines also placed (coverage-gaps 2 -> G-coverage-gaps-01, unproven-checks 2 -> -08, vacuous-gates 3 -> -03)). 24 items: severity high 10 / medium 14; kind weak-spot 11, defect 4, danger-zone 4, unfinished-promise 3, improvement 2 (both with measurable targets); disposition classified third-party 6 (upstream guard) + open 18 (12 blocked). Largest: G-unsealed-evidence-01 68 rows; G-unproven-checks-02 64 rows (the CONST-050(B) challenge set copied into 8 submodules); -05 12 rows; -01 11 rows (the no-suspend scanner, 3 different versions across the copies). Hand-verified live: all 26 guard rows piped as data — exactly the 21 baseline ids exit 0 and the 5 control rows exit 2; all 103 unproven-checks rows have no proof arm and no sibling proof (whether each *_challenge.sh is really a gate: unmeasured); all 16 unregistered scripts, 4 blind-window lines, 2 vacuous-gates lines; anchor, evidence store and coverage.tsv absent. OPERATOR DECISIONS raised: (1) register the feature's own unfinished tasks (T065, T069, T038 freeze, T052) as items (unsealed-evidence-01/02, coverage-gaps-01)? tracks them twice; (2) are doc_processor, llm_orchestrator, llm_provider, vision_engine, qa OWNED by us (open, fixed in each module repo, one gitlink bump per module) or third-party? 8 affected modules also lack a roster prefix (T047/V-G8); (3) grouping: one item per cross-repo template family vs 13 per-repo items (clashes with one committer per repository), guard-gaps 6 items vs 1; (4) accept third-party classification for the six upstream guard items, approve project-level mitigation G-guard-gaps-07 (edits .claude/settings.json), rate host-power (high or critical) and bulk staging (high or medium); (5) category/type: the class files unproven-checks as test-coverage but T045 puts them in the false-evidence wave (decides which wave owns 103 rows); research D3 has no type for weak-spot (11 items) — proposed Bug. No plan_due dates proposed (operator's). Output flagged instruction-shaped by the harness only for mentioning .claude/settings.json (a plain mention). SUMMARY.md could not be written by the subagent (Write refuses subagent report files) — its content is in this entry."
  - "Seed dry-run seed-build DONE (nothing written; DB sha 9413fd96f2647c63; files /tmp/zg-seed/build/: items.tsv 20 items, members.tsv 209 = 208 baseline rows + 1 live-only row progress.yml:95, coverage.txt: build-if-missing 8/8, missing-toolchain 4/4, live-vs-source 3/3, private-in-public 7/7, content-boundary-rows 186/186, total 208/208, 0 dropped; content-boundary groups sum to 14621 = cache counts.leaks). Severity 1 critical / 10 high / 6 medium / 3 low; kind 8 defect, 8 danger-zone, 2 improvement, 2 weak-spot; disposition 10 open + 10 classified (8 operator-decision, 1 operator-action, 1 third-party). Live re-run of the five classes matches the baseline except private-in-public 8 and content-boundary-rows rc 2 'cache stale: 71 files differ'. CRITICAL (NOT in the baseline; found by running the class over specs/002-009, OUTSIDE its population, where its detector matched 1 of 15 lines): TWO ACCOUNT PASSWORDS are written in plain text in public spec 007 — 15 lines across 5 files, first committed ed9a34a on 2026-09-12 and pushed to all three remotes; the same values appear in 13 ai_interviewing and 15 workshop test files. Whether they are the LIVE passwords is UNCONFIRMED (no login attempted). The only remedy for a published value is ROTATION of both passwords (history cannot be rewritten, §11.4.113). PROCESS SLIP (§11.4.10): one seed-build check command printed one such spec line unmasked into that subagent's tool output; it is in no file and MUST NOT be reproduced anywhere — locations are recorded by path only. Follow-ups: extend the private-in-public population to specs/002-009 + a masked-output rule for verification commands (a class/gate must never print a matched credential); a secret-scanner recall gap (1 of 15 lines matched). Judgement calls: G-live-vs-source-01 proposed medium (not the class's high: served build and HEAD differ only in 3 test files) and operator-action (a redeploy); content-boundary: one decision item (packet options 1, 2 or 4; option 3 shipped 2026-09-09) + 7 path-class groups + 1 item for the 137 files the gate never indexed, every group waits on the operator (judging a row means reading private material), constitution group third-party, public site content group (11 name rows, one per language) flagged to read first; private-in-public: 6 baseline rows + the live-only row become ONE detector-improvement item (live precision 1 of 8), the one real row sweep-classes.tsv:21 proposed low; fixes ending in a submodule pointer update (G-build-if-missing-01 and -04, G-live-vs-source-02) are open, blocked on that update; test-setup-agents-wizard.sh:666 records a FAIL (not rc 2) when lumen is absent — kept inside G-missing-toolchain-01."
  - "Seed dry-run seed-docs DONE (register untouched; only a read-only copy opened, -wal 0 bytes): PROPOSED-ITEMS.tsv 32 items (30 with rows + 2 derived with 0 rows), MEMBERS.tsv, COVERAGE.txt VERDICT COVERED rc=0: all 816 rows accounted for (814 in items, 2 dropped: progress.yml:68 and :77 review-log lines; stale-figures 522=522, doc-count-drift 238=238, pointer-drift 23=23, known-open-decisions 1+2 dropped=3, improvement-candidates 30=30), HANDCHECK.tsv 74 hand checks all PASS (5 confirm a drop/FP/dated note). Items: severity 3 high / 19 medium / 10 low; kind 19 weak-spot, 6 improvement (all with targets), 4 unfinished-promise, 3 defect; disposition 28 open, 4 classified operator-decision. The three highs: README badge says 23 vs the ledger re-measure printed 41; workshop carriers record 23/27/20 vs 101/94/47 measured; a workshop gate passes without reading the prose it guards (derived, UNCONFIRMED). Judgement calls: (1) CONTINUATION.md §3 394 rows are dated session-log figures — reasoned point-in-time rule with proof the other sections stay covered, or archive old entries (no ledger rows proposed); (2) 225 low 'unmeasurable count' doc-count-drift rows: 7 items one per owning repo (9 of 11 repos need a per-bump gitlink decision) vs treat as a class limit and do not seed; (3) 18 rows point at EXISTING register items (VSC-028, VSC-285, WSP-151 + 15 Queued) — fill gap fields on the existing ids instead of gap add (needs a writer that keeps the gap fields; canonical close wipes them); (4) submodules behind remotes: LLMProvider, llm_provider, llms_verifier = three separate per-bump decisions; constitution pin open under the standing authorization (remote moved again today to 0ff780dc4e4f); are llm_provider (HelixDevelopment) and LLMProvider (vasic-digital) meant to be two gitlinks (both remotes report the same tip)?; (5) six doc-count-drift FPs/dated notes kept as evidence for a detector-fix item. Line numbers are those of f05817ba, so claim-key is the stable key. No plan_due proposed. TOTAL across the three seed proposals: gates 215 + build 208 + docs 816 = 1239 = the baseline row count; 76 proposed items (24 + 20 + 32). Proposals copied (paths only, scanned: no credential values, no /home/) into specs/010-zero-gap-verified-closure/baseline/seed-proposal/{docs,gates,build}/."
  - "Migration dry-run plan-migrate DONE (register untouched; DB sha 9413fd96f2647c637d1581a54dd7038bcc0c980cc66c4b1c40650228f0d781f6 unchanged; work under /tmp/zg-migrate/ only). (1) 8 roster gaps confirmed via .gitmodules+helix-deps.yaml, proposed prefixes (convention: docs/workable-items/sub-projects.tsv header + existing rows, first-letters-of-basename style, no collisions with the 15 existing): HQA submodules/qa, CHA submodules/challenges, DOC submodules/doc_processor, LLO submodules/llm_orchestrator, HLP submodules/llm_provider (HelixDevelopment — distinct from already-registered LLP=submodules/LLMProvider vasic-digital, CLASH RISK operator must pick a different code for one of the two), LMV submodules/llms_verifier, SEC submodules/security, VSE submodules/vision_engine. (2) migration sequence demonstrated end-to-end on a copy: backup (cp -a; hardlink failed cross-device /home->/tmp, fell back to plain copy, documented) -> roster (rc 2->0) -> seed-roster (rc 0, 23 rows) -> gap migrate --dry-run then real (rc 0, 15 DDL statements; second run 0 statements = idempotent) -> backfill (rc 2 COULD-NOT-DETERMINE on the minimal copy — honest, no-op on the real live tree since provenance is already backfilled) -> validate (rc 0 clean). (3) confirmed: migrating WITHOUT the roster fix first flips validate/G5 rc 2->1 (8 real V-G8 findings); migrating WITH the roster fix pulled forward goes rc 2->0 clean, never showing rc 1 — CONFIRMS the operator's chosen ordering (roster before migration) is correct. verify-workable-items.sh is NOT called from pre-push-gates.sh (0 hits) — it is a standalone check-registry.tsv row (line 290) with its own --prove-failure, so migrating never blocks pre-push or commit/push_all.sh; verify-claim-ledger.sh and continuation-check.sh have zero references to workable_items, neither flips. (3b) 5 real gap add calls against the copy succeeded rc 0 (VSC-348 defect, VSC-349 improvement with --measurable-target, CON-002 defect/security then classify --reason third-party rc 0, VSC-350 danger-zone, VSC-351 unfinished-promise); full validate after all 5 + classify: rc 0. TSV->flags mapping: severity/category/kind/type_D3->type/title/root_cause+proposed_fix->description/proposed_fix->closure-criteria/plan_due_or_recheck->plan-due/measurable_target/sample_locations->anchor+evidence. (4) CONFIRMED TOOLING GAP: no gap subcommand sets it.gap=true on a pre-existing row except add's INSERT (gapcmd.go:448,664 — classify/close/verdict all refuse !it.gap), so the 18 rows pointing at existing items (VSC-028, VSC-285, WSP-151 + 15 more) CANNOT be adopted without either minting a duplicate id or a canonical close that wipes columns — needs a NEW `gap adopt --id <existing>` subcommand (UPDATE-only, mirrors close's column-preserving pattern) or a reviewed one-off SQL UPDATE; this is new implementation work, not yet done. gap freeze --cycle 1 demonstrated last: rc 0, docs/zero-gap/cycles/1/freeze.json written, 5 items, register+members digests recorded. (5) rollback = the verified plain-copy backup; re-verify after any real migration with DB sha + verify-workable-items.sh + verify-manifest-pins.sh + verify-governance-cascade.sh. OPERATOR DECISIONS: ratify the 8 prefixes (HLP/LLP collision risk flagged); decide the gap-adopt writer design before T038 seeding of the 18 existing-item rows; improvement-cap/disposition/category/wave-ownership decisions from the seed dry-run entries remain open."
  - "Operator ratifications 2026-09-26: (1) keep HLP for submodules/llm_provider despite the visual closeness to LLP (submodules/LLMProvider) — all 8 prefixes RATIFIED as proposed: HQA submodules/qa, CHA submodules/challenges, DOC submodules/doc_processor, LLO submodules/llm_orchestrator, HLP submodules/llm_provider, LMV submodules/llms_verifier, SEC submodules/security, VSE submodules/vision_engine. (2) build a new `gap adopt --id <existing>` subcommand now (column-preserving UPDATE, mirrors close's pattern) rather than a one-off SQL UPDATE or deferring the 18 existing-item rows — dispatched to an implementer, test-first, independent review required before use in seeding."
  - "tasks.md correction pass DONE (fix-tasks): grep -c '^- \\[' 80 -> 90, all ids unique, every T### reference resolves, audit-hardcoded-paths rc 0, existing checkbox states untouched. New: T082 (N3 fix, in progress, before T068), T083 (first tracked anchor.json, Operator-blocked, gates T068 --verify and T074), T084 (early migration + 8-prefix ratification, before T038 in Phase 3 — resolves the T038/register-cli.md deadlock), T085 (private-in-public extension to specs/002-009 + masked-output rule), T086 (operator queue: rotate the two exposed passwords, named by reference only), T087 (coverage-gaps re-sweep after T052, new findings to cycle 2 not a reopen), T088/T089/T090 (test-coverage / other / kind=improvement waves, cycle-1 scope = all 10 items per the 2026-09-26 decision), T091 (CONTINUATION.md §3 point-in-time rule, wired to T019 stale-figures and T047 docs-drift). Line edits: T032 notes T087 re-sweep; T081 cap removed (all 10, no cap) pointing to T090; T038 now after T084 with the ratified scope stated; T047 reassigns roster/migration to T084 and states the OWNED-module ruling (doc_processor, llm_orchestrator, llm_provider, vision_engine, qa: fixed per-repo, one gitlink bump each, operator decision) vs in-tree closure for challenges/llms_verifier/security; T050 names the 5 owned-module bumps + T086; T051 vs T073 disambiguated (T051 = one-time RED/GREEN evidence-pair audit from Phase 4; T073 = re-executes the actual check for every closed item after Phases 5-7); T062 push-path placement clarified (after T041, before T065); T068/T069 corrected (N3 -> T082, daily witness fetch -> T069, T068 also depends on T083); Dependencies/Execution-Order and Implementation-Strategy sections rewritten (US2/US4/US5 bullets fixed, MVP checkpoint T036->T038, RECOMMENDED FIRST WAVE list added matching the operator-endorsed order)."
  - "T014 N3 fix DONE (impl-n3): _tools/zero-gap-chain/history.go, new fix5_test.go, scripts/zero-gap-evidence-chain.sh. ROOT CAUSE measured (not guessed): remote.<r>.uploadpack spawns the named program DIRECTLY on this host for a local/file-transport remote, never on the remote's side (old comment was wrong). DEVIATION FROM THE DISPATCHED FIX TEXT, empirically required and RULED ACCEPTED: `-c remote.<r>.uploadpack=git-upload-pack` does NOT override a malicious config (git errors 'more than one uploadpack given, using the first' and still runs the configured program, verified with a marker script) — the construct that actually wins is the `--upload-pack=git-upload-pack` command-line flag, used instead. Per-vector RED->GREEN, each reproduced with a marker script first: remote.<r>.uploadpack local-transport exec -> --upload-pack=git-upload-pack; submodule recursion (real fixture, populated submodule with a marker uploadpack, submodule.recurse=true) -> --no-recurse-submodules; core.fsmonitor at TWO independent sites (inside gitFetch itself, actually silenced by --no-recurse-submodules alone per git's submodule-recursion detection, closed defense-in-depth with -c core.fsmonitor=false anyway; and the separate git ls-files call in VerifyAnchorHistoryWith, genuinely independent, closed with its own -c core.fsmonitor=false); --no-auto-maintenance added per spec (argv-capture test only); url.<x>.insteadOf->ext:: already blocked by the existing GIT_ALLOW_PROTOCOL=file:https:ssh (regression test added, no code change). DELIBERATELY LEFT OPEN, RULED and test-pinned rather than silently undocumented: core.sshCommand and credential.helper (RED confirmed via marker over ssh://) — closing either breaks real operator ssh/credential setup for the three umbrella remotes; TestN3_WitnessFetchSshCommandResidualIsDocumentedNotClosed pins this. GIT_SSH_COMMAND/GIT_PROXY_COMMAND/GIT_EXEC_PATH/PATH: reviewed, not invoked by fetch or intentionally left (inherited pre-existing design). 5 mutations tried (missing --upload-pack, missing --no-recurse-submodules, missing --no-auto-maintenance, missing fsmonitor override at each of the 2 sites) — all 5 caught by at least one test. go test ./... (GOFLAGS=-count=1): all packages PASS 0 failures (6 new TestN3_* cases + all pre-existing). Shell --prove-failure: 84/0 (was 77/77; added H16 uploadpack, H17 fsmonitor, H18 insteadOf->ext, H19 documented sshCommand residual). go build/vet clean. verify-check-registry rc 0 (124 PASS/0 FAIL/0 DEBT/0 UNDET); audit-hardcoded-paths rc 0 (pre-existing baselined debt only, unrelated submodules). Header/comment texts corrected in both files (local execution not on-the-remote's-side; residuals split from closed vectors with the operator-ruling reason inline). CONTROLLER NOTE: T082's dispatch text in tasks.md names the wrong flag (`-c remote.<r>.uploadpack=...`) — correcting the task text to `--upload-pack=git-upload-pack` (a wording fix only, not touching checkbox state or other tasks). Independent review needed before T068 relies on this."
  - "T014 N3 review rev-n3: ACCEPT-AFTER-FIXES. Every implementer experiment reproduced from scratch in an independent scratch repo and confirmed exactly: -c remote.<r>.uploadpack does not override (git errors 'more than one uploadpack given, using the first', marker still runs), --upload-pack=X on argv wins (confirmed); --no-recurse-submodules stops a real populated-submodule marker; go test 76/0 (all 6 TestN3_* green), go build/vet clean; shell --prove-failure 84/0 exact match; header/comment corrections now factually accurate; hygiene clean. IMPORTANT (fix required): credential.helper has NO pinning test anywhere (grep across *.go finds only the doc comment; fix5_test.go has TestN3_WitnessFetchSshCommandResidualIsDocumentedNotClosed for sshCommand but nothing analogous for credential.helper; no H20 shell case) — progress.yml's claim 'core.sshCommand and credential.helper (RED confirmed via marker over ssh://)' OVERSTATES what was pinned: credential.helper is an https-transport mechanism, not reachable over ssh://, and reviewer needed a real local HTTP server returning 401 to trigger it (DNS failure alone does not reach the credential subsystem) — add a Go test (and an H20 shell case) pinning it as documented-open, matching the sshCommand rigor, or correct the claim. Minor (informational, not blocking): NEW FINDING not in the implementer's report — if .git/refs/zero-gap/witness is ALREADY a symlink to an outside directory (pre-existing write access to this clone's .git required), the fetch follows it and writes the loose-ref file outside .git/refs entirely; add one sentence to the RESIDUAL block noting this is the same as any other git ref write, not a new privilege the fetch grants. No fabricated evidence found; every re-run quantitative claim matched exactly."
  - "gap adopt subcommand DONE (impl-adopt): _tools/workable-items/internal/wi/gapcmd.go (new gapAdopt, ~195 lines) + new adopt_test.go (8 test funcs, 235 lines). Design: `gap adopt --id <existing> --kind --severity --category --title --root-cause --proposed-fix --closure-criteria [--anchor --owner --evidence --plan-due --measurable-target --disposition open|classified ... --as-of]` — a column-preserving UPDATE (never INSERT) mirroring `add`, preserving atm_id/type/status/current_location exactly, filling the 14 gap columns; added `--owner` (not in the originally dispatched flag list) because V-G1 requires a non-empty owner and legacy rows carry none. Refuses: missing id, already-gap id (it.gap guard), missing required field, kind=improvement with no --measurable-target (V-G4), disposition mismatching the item's current status (adopt never changes status). body_md untouched; item_provenance uses INSERT OR IGNORE so pre-existing provenance is never overwritten. TDD: RED (function absent) -> GREEN for happy path + every refusal case, tested against a migrated legacy-register.sql copy. Coverage: happy path (VSC-002: ValidateGap clean, report --by-module places it under its module not Unplaced), double-adopt refused (both a just-adopted item and a gap-add'd one), nonexistent id refused, each of the 7 required flags refused individually, status/disposition mismatch refused (VSC-001 closed legacy), ISOLATION proven (VSC-001 fully unchanged after adopting VSC-002, 13 columns diffed), ATOMICITY proven via a new adoptMidHook test seam triggering a real SQLite CHECK-constraint violation mid-transaction (rc 2, gap columns stayed empty, then a clean retry succeeded). Mutation: removed the it.gap guard, TestGapAdoptRefusesAnAlreadyGapItem FAILED (rc 0, silently re-adopted) as expected, restored -> PASS. go build/vet clean; go test ./... only the pre-existing TestRosterLoadsAndCrossChecksTheLiveFleet fails; verify-check-registry rc 0 (124 PASS); audit-hardcoded-paths rc 0; live DB sha unchanged. DRY-RUN CONFIRMED against a migrated copy: gap adopt succeeded (rc 0) for VSC-028, VSC-285, WSP-151 — 3 of the 18 seed-proposal existing-item rows — leaving atm_id/type/status/current_location untouched; repeat-adopt of VSC-028 correctly REFUSED rc 1. Independent review needed before this is used to seed the live register."
  - "T085 DONE (impl-t085): only scripts/zero-gap-class-private-in-public.sh touched. TDD: baseline 36/0, RED confirmed with new M2d/M2e/M2f (masked-output marker) + M16a-d (population extension) assertions added first (5 failures), GREEN after the fix at 43/0. Population extended: raw_items() adds specs/00[2-9]-* to the git ls-files call (specs/001-*/ and other specs/010-*/ besides this feature stay excluded, verified by M16). Masked-output rule (FR-020): AWK_DETECT tags each credential shape as shape:len (len = the withheld value's own length, not the whole match — fixed an AKIA boundary-group over-count bug found during GREEN, 21->20 length); the description builder now emits 'shape [REDACTED len=N]' or bare [REDACTED]; the old sha256:<digest-of-whole-line> disclosure REMOVED entirely — this was the sole text source for findings/--json/register descriptions, no other file needed changes. Verified: corpus recall exact (24/24), 3 live runs byte-identical, registry/hardcoded-paths rc 0. Live re-measured: guessing 55 hits (unchanged, now one population), content-boundary 0 (unchanged), credentials 1 live hit — specs/007-decouple-modules-auth/spec.md:184, description 'constitution-credential-scanner [REDACTED]', NO value/digest/excerpt printed in 3 repeated runs (confirmed read-only); this is the ALREADY-KNOWN recall gap (only 1 of ~15 real password lines is shape-detectable by the generic layer; the rest need a separate recall-improvement follow-up, explicitly out of scope for T085) — population 44->188 records, 149 inspected, 39 CNI (mostly binary PDFs). CONTROLLER EDIT APPLIED: sweep-classes.tsv private-in-public row appended '; plus specs/002-*/ through specs/009-*/ (T085 extension, FR-020)'."
  - "T014 N3 round 2 DONE (impl-n3): credential.helper pinning fixed for real — RED confirmed the naive approach (credential.helper + unreachable DNS host never invokes the helper, exactly the false-negative rev-n3 flagged) -> GREEN with TestN3_WitnessFetchCredentialHelperResidualIsDocumentedNotClosed using a real httptest.NewTLSServer answering 401 (the helper genuinely fires once the HTTP layer gets a real 401); mutation check (remove the credential.helper config line) fails as expected, restore passes. Matching shell case H20 (new `attk httpsrv401` embedded Go subcommand starts the same local TLS 401 server) added; --prove-failure 85/0 (was 84). RESIDUAL comment blocks in history.go and the shell header corrected to stop conflating core.sshCommand (ssh://) and credential.helper (https://, needs real reachability) as one item — each now has its own bullet and pinned test name. Symlink note added (no code change) to both RESIDUAL blocks per the minor item. go build/vet clean, go test ./... all green (7 TestN3_* cases, ~13.7 s), --prove-failure 85/0, verify-check-registry rc 0, audit-hardcoded-paths rc 0. Scoped re-review dispatched (rev-n3-2) before ticking T082, given this is a security-sensitive witness-fetch chain."
  - "gap adopt review rev-adopt: ACCEPT-AFTER-FIXES. CRITICAL (§11.4.253 violation, empirically reproduced): NO DB-level uniqueness guard — the it.gap refusal reads via loadItem BEFORE the write transaction (TOCTOU window), and the UPDATE's WHERE clause has no condition guarding against a row already adopted; two CLI processes racing on VSC-002 with the window widened BOTH printed 'adopted rc 0' and the register ended up with 2 zero-gap:adopted history rows for one id — adopt's one-time invariant breaks under real concurrency, exactly the seeding scenario (18 rows, possibly multiple concurrent agents). Fix: add `AND disposition IS NULL` (or `AND kind IS NULL`) to the UPDATE WHERE clause, check RowsAffected()==1, else refuse 'already adopted concurrently' and roll back. IMPORTANT: contracts/register-cli.md and data-model.md have ZERO mentions of adopt (grep -i confirmed) — only adopt_test.go and the progress.yml note describe the flag set including the scope-creep --owner addition; docs must be updated. Verified clean (strengthened beyond the implementer's own tests): isolation re-diffed ALL 37 columns of VSC-001 AND a freshly planted third row VSC-500, zero diffs both; atomicity via a REAL OS-level kill -9 mid-transaction (process verified via ps, killed after the items UPDATE ran but before commit) — PRAGMA integrity_check ok, VSC-002 fully reverted, stronger than and confirms the CHECK-constraint hook test; V-G rules: gapCleanIn runs the FULL ValidateGap (V-G1-V-G12), not just V-G1/V-G4 (adequate, though no test names V-G3/V-G7/V-G8 individually); SQL injection/malformed input (quote, unicode, 65536-byte root-cause) all round-tripped correctly via parameterized queries; item_provenance INSERT OR IGNORE confirmed genuinely preserved (byte-identical after adopt) by direct query, not trusted from the claim; hygiene clean. SEEDING READINESS: NOT safe for unattended/concurrent seeding of the 18 rows until the critical fix lands; sequential single-process seeding is safe today (the race requires genuine process concurrency). Fix #2 (docs) not a safety blocker but required before contract-complete."
  - "T091 DONE (impl-t091): docs/zero-gap/point-in-time.tsv, scripts/zero-gap-class-stale-figures.sh, scripts/zero-gap-class-doc-count-drift.sh. Reused mechanism: doc-count-drift's existing point-in-time.tsv reader (fnmatch path-match + reason column, no-reason=>CNI never silent) — added a new INTERPRETATION not a new list: a row whose reason contains the literal phrase 'own dated caption' caption-gates instead of blanket-exempting — only lines under a '### … — YYYY-MM-DD' heading within the document's '## … §3 …' section are exempt, an entry heading with no date stays a finding, implemented identically (captioned()) in both scripts. stale-figures gained point-in-time.tsv support for the first time (previously none), reading only caption-gated rows; doc-count-drift never scans CONTINUATION.md (boundary ruling) so the new row is applied by stale-figures only; doc-count-drift proven to treat a caption-gated row matching none of its own documents as out-of-scope, not a CNI, via its own corpus. RED->GREEN stale-figures (--prove-failure 37/0): M9c baseline both dated-caption and identical uncaptioned line fire; M9 after the row: captioned exempt, uncaptioned near-miss still fires; M9e reasonless row => CNI. doc-count-drift (PROOF PASSED): P15b both lines fire, P15 only uncaptioned fires. Live CONTINUATION.md (stale-figures) re-measured: medium findings 418->316 (-102, 0 new), LOW aggregate line-count 479->364; all 102 removed lines fall inside §3 (lines 484-9831), ZERO removed at/after line 11151 (§4+) — confirms section scoping held; uncaptioned §3 entries (e.g. a 'LESSON LINK...ROUTES' entry with no date) unaffected, 0 new findings anywhere. 3 live runs byte-identical both classes, corpus recall exact, bash -n clean, registry/hardcoded-paths rc 0. No controller tsv edit needed (population wording already covers point-in-time.tsv)."
  - "T014 N3 round-2 scoped re-review rev-n3-2: ACCEPT for T082 overall. Independently reproduced both RED-without-fix and GREEN-with-fix (not trusted from the report): the credential.helper test mutated (config line commented out in a scratch mirror) fails exactly as expected, restored passes; the httptest 401 server independently probed and confirmed genuinely answering 401 (not a lucky pass). H20 confirmed genuinely exercised: prove-failure 85/0 on the real repo, dropped to 84/0 when H20 was commented out in a scratch mirror — exact -1, not vacuous. RESIDUAL blocks in both files confirmed correctly separate sshCommand (ssh://) from credential.helper (https://, needs real reachability) with each naming its own pinned test. go build/vet clean, go test ./... 14.3 s no failures, all 7 TestN3_* individually verified PASS. Round 1's genuine gap (doc comment with no pinning test) is now closed. Safe to rely on before T068 uses it."
  - "T052 DONE (impl-t052): scripts/zero-gap-coverage.sh (477 lines, new) generates docs/zero-gap/coverage.tsv (253 lines: header + 252 cells), one row appended to check-registry.tsv. Subjects derived by REUSING (not reinventing) the same ownership algorithm doc-count-drift and cascade C1 already use — .gitmodules URL namespace with >=2 declared submodules or a helix-deps.yaml ssh_url, excluding the governance source and third-party, excluding anything not checked out: 21 subjects x 12 required kinds (unit/integration/e2e/contract/security/accessibility/perf-load/stress-chaos/mutation/determinism/regression/manual-qa-readiness, the coverage-gaps class's own REQ_KINDS, no extras). Live status: 3 check (root/unit ref _tools/gen/go.mod, real go test ./... 34 PASS parsed; root/mutation ref mutation-anchor-rot; root/security ref pretooluse-guard, each parsed for a real N-passed/N-caught count), 249 gap, 0 could-not-run, 0 n/a. MEASURED (not guessed) unsafe to broadly go-test owned submodules here: submodules/qa fails outright (go workspace-mode conflict), submodules/challenges did not return inside 30 s — generator reports those cells gap rather than risk hangs/network calls; deeper per-module harnesses are T053's job. REAL BOUNDARY FOUND by actually running the consumer class: 3 owned modules (challenges, llms_verifier, workshop) carry real docs/qa/ evidence, but the coverage-gaps class resolves a check ref's tracked-file half via git -C <root> ls-files, which never sees inside a submodule (gitlink boundary) — an earlier generator draft cited those refs and coverage-gaps correctly flagged 'high false-evidence'; fixed by crediting manual-qa-readiness only at the root subject (root itself has no docs/qa files -> gap there too), documented in the header, covered by proof case C5 (a module's real docs/qa file stays gap, root's becomes check). decide_status() reuses the SAME hollow-proof vocabulary (N assertions|mutations|caught|passed) that verify-check-registry.sh --run-proofs already uses, rather than reinventing vacuous-gates' static source-scan (its instrument doesn't apply to generated runtime output; its principle — zero executed cases is not a pass — does). --prove-failure 18/18 (RED-first on decide_status/parse_count, subject derivation incl. governance-source/singleton-namespace/not-checked-out exclusions, the manual-qa-readiness boundary fix, determinism, --root /nonexistent rc 2, output vocabulary closure, live-tree generation byte-identical before/after). verify-check-registry rc 0 (126 PASS, new row's SC-012/SC-013 both PASS); audit-hardcoded-paths rc 0. coverage-gaps class against the new coverage.tsv: no longer permanently CNI — exits 1, 249 FINDING coverage-gaps medium test-coverage rows, 0 false-evidence, 0 CNI, INSPECTED 252. Independent review needed (this data feeds the register)."
  - "T052 review rev-t052: ACCEPT-AFTER-FIXES. Every headline claim independently reproduced with real runtime evidence: --prove-failure 18/18 rc 0 6.0s; subject derivation hand-re-derived from .gitmodules+helix-deps.yaml (namespace counts vasic-digital=11, helixdevelopment=6 incl. governance source, milos85vasic=4, wangx0111=1 singleton/unpromoted) = exactly 20 owned + root = 21 subjects x 12 kinds = 252 cells, matches; algorithm line-by-line compared against doc-count-drift's gitmodules()/namespace() — faithfully reused, no divergence, no borderline miss found; the 3 check cells re-run independently and matched (34 PASS, mutation-anchor-rot '1 caught/5 assertions' parsed to 1, pretooluse-guard '15 passed/0 failed/15 cases' parsed to 15); manual-qa-readiness gitlink-boundary finding CONFIRMED real (challenges/llms_verifier/workshop have real docs/qa content, git ls-files at root genuinely returns EMPTY for it, root-only-crediting fix is correct not symptom-avoidance); coverage-gaps against the live tsv rc 1/249 medium/0 false-evidence/0 CNI/INSPECTED 252 exact match; regenerated coverage.tsv byte-identical to committed; registry/hardcoded-paths rc 0; hygiene clean. IMPORTANT (fix before register intake): parse_count is a presence-vocabulary detector doing count-EXTRACTION duty and can be fooled — `parse_count 'x' '0 of 5 mutations attempted'` returns 5 not 0 (the regex only requires <digit> <space> <trigger word> anywhere, no requirement the number denotes a genuine positive count vs attempted/planned/expected text preceding it); TODAY's 2 live checks are not exposed because both call sites gate parsing behind rc==0 of the underlying proof (their real N-caught/N-passed line is the first match and rc is genuinely 0 only when genuinely all-pass) but this is a narrow non-structural mitigation — any FUTURE check wired in whose proof text mentions a larger number+trigger-word before its real result line, while still exiting 0, would silently mis-credit a cell. Fix: require the number immediately adjacent to `caught` or `passed` specifically (not `mutations?`/`assertions?` alone), or take the count nearest a line-anchored summary pattern. IMPORTANT (log as known gap, defer to T087/T053 if preferred, do not silently assume covered): fabrication of coverage.tsv is only PARTIALLY detectable — coverage-gaps verifies a check cell's ref resolves but never verifies the ref is RELEVANT evidence for that subject+kind (a hand-edited row pointing at any real tracked file for an unrelated subject would sail through); subject-set completeness is untested downstream (coverage-gaps' population comes solely from coverage.tsv's own subject column, no cross-check against .gitmodules-derived ownership truth, so a dropped/fabricated subject row goes unnoticed); nothing outside the generator's own --prove-failure references it, and that proof's live-tree assertion only checks structural well-formedness of a throwaway regeneration, never diffs against the COMMITTED file — the generator's honesty currently rests on trusting this one run, mitigated only by the reviewer's manual live diff (not a standing mechanical gate). Recommend a future task wire a regenerate-and-diff-against-tracked-file gate. Both findings are genuine defense-in-depth gaps, NOT evidence any of the 3 check or 249 gap cells are currently wrong — every reproduced number matched exactly."
  - "gap adopt race fix DONE (fix-adopt): CRITICAL TOCTOU (§11.4.253) fixed. RED reproduced first, not assumed: two REAL independent OS processes (os/exec-spawned, not goroutines) racing gap adopt --id VSC-002 against the same on-disk register, with a new env-gated adoptRaceWindow() (WI_GAP_ADOPT_RACE_MS) to widen the window — both printed adopted rc 0, register ended up with 2 zero-gap:adopted history rows for one id, exactly rev-adopt's finding. Fix — exact WHERE-clause change: `... WHERE atm_id=? AND current_location=?` -> `... WHERE atm_id=? AND current_location=? AND kind IS NULL` (kind is one of the 14 additive gap columns, gapAdopt is its only writer, every item passing the pre-tx it.gap check has kind IS NULL at that instant, so the guard matches only until some transaction commits a non-NULL kind); RowsAffected() now a 3-way switch: n==0 -> rollback + refuse rc 1 'adopted concurrently by another process; nothing was written'; n!=1 (unexpected) -> envFail rc 2; n==1 -> proceeds. GREEN: identical two-OS-process recipe against the fixed binary gives one rc 0 adopted, one rc 1 refused, exactly 1 history row, kind=the winner's write only. Permanent test TestGapAdoptRaceRefusesConcurrentDoubleAdopt (goroutines + a new real transaction-ordering hook adoptPreBeginHook firing right after the it.gap check, before e.begin(); built with atomic.CompareAndSwapInt32, NOT sync.Once — Once.Do deadlocked the first draft since it blocks the second caller until the first returns, which only happens after the second completes) — 20/20 repeat runs pass, clean under -race. Mutation: removed AND kind IS NULL, re-ran the test 3x, reliably RED (rc=0/0, 'want exactly one rc 0 and one rc 1'); restored, verified via diff against a saved fixed copy. Docs: contracts/register-cli.md gained `gap adopt` in the command table + a full Amendment section (every flag incl. --owner, one-time/status/isolation/atomicity behavior) + a Concurrency (§11.4.253) subsection explaining the guard and pointing at the test; data-model.md correctly left untouched (no command-reference section there). Test counts: 9 adopt test functions all pass incl. the new race test; go test ./... only the pre-existing TestRosterLoadsAndCrossChecksTheLiveFleet fails; go build/vet clean, gofmt -l clean; verify-check-registry rc 0 (126 PASS); audit-hardcoded-paths rc 0; live DB sha unchanged, never opened by any test (copies only). Scoped re-review dispatched (rev-adopt-2) before this is used to seed the live register."
  - "T052 parse_count fix DONE (fix-t052): RED confirmed first with 4 new --prove-failure cases (C3e-C3h: '0 of 5 mutations attempted'->wrongly '5', '5 mutations planned, 0 caught'->wrongly '5' not '0', 'expected 10 assertions'->wrongly '10', '10 mutations expected, 3 caught'->wrongly '10' not '3'); 18 PASS/4 FAIL RED, 22 PASS/0 FAIL GREEN. Fix: replaced grep-based parse_count with a word-scanning awk implementation, same trigger vocabulary (assertion(s)/mutation(s)/caught/passed) — a number directly followed by 'caught' or 'passed' is trusted immediately; a number adjacent to 'assertion(s)'/'mutation(s)' alone is trusted ONLY when neither the preceding word nor the word following the trigger is attempted/planned/expected, else skipped and the scan continues rightward (so 'planned...0 caught' still finds the real 0); no match -> unparseable -> gap, never fabricates a count. Live check cells re-confirmed unaffected: mutation-anchor-rot -> 1, pretooluse-guard -> 15 (both exact). coverage.tsv regenerated for real and diffed (cmp + git diff): byte-identical, no change (the fix only alters attempted/planned/expected-adjacent phrasing, absent from both live checks' real proof text). --prove-failure rc 0 (22/0), bash -n clean, verify-check-registry rc 0 (126 PASS), audit-hardcoded-paths rc 0. rev-t052's coverage.tsv-diff-detectability finding remains DEFERRED as instructed. Scoped re-review dispatched (rev-t052-2)."
  - "gap adopt race fix scoped re-review rev-adopt-2: ACCEPT. gap adopt is now SAFE for concurrent/parallel seeding of the 18 rows. Independently re-derived every claim from scratch: real two-OS-process race (own binary, own scratch-migrated copy, WI_GAP_ADOPT_RACE_MS=800) gives rc=1/rc=0, exactly 1 history row, kind matches only the winner — reproduced 15/15; mutation kill (AND kind IS NULL removed in a scratch copy) reliably reproduces the original defect (2 duplicate rows) on demand; go test -race ./... clean, no data race anywhere (only the pre-existing unrelated roster test fails, as noted); NEW scenario 4 tested: add-vs-adopt on the same id is a NON-ISSUE BY CONSTRUCTION, verified empirically both orderings — gap add's id-max query + INSERT run inside one BEGIN-IMMEDIATE transaction (_txlock=immediate) and sets kind in the same statement, so a fresh id never exists with kind NULL; raced add (minting VSC-003) against adopt --id VSC-003 15x, adopt always got a clean 'no item' refusal (its read landed before add's commit); forcing the reverse ordering gave the expected 'already a gap item; one-time' refusal; both paths pre-existing, unrelated to the race fix, never corrupt state. NEW scenario 5: three-way race (3 processes, same id) gives exactly 1 rc 0 / 2 rc 1, exactly 1 history row, PRAGMA integrity_check ok. Refusal message confirmed adequate for scripted seeding (distinct wording for lost-race vs nonexistent vs already-adopted). contracts/register-cli.md's Concurrency section re-read against all of the above: accurate. Hygiene clean, DB sha unchanged. No further gaps found in the race fix specifically."
  - "T052 parse_count fix scoped re-review rev-t052-2: ACCEPT. --prove-failure self-run twice, rc 0, 22/0 stable. All 4 adversarial phrasings re-tested directly against parse_count (bypassing the harness), matching C3e-h exactly. False-negative hunt: 6 real proof-output-shaped lines from this repo's own scripts (verify-docs-chain.sh, verify-claim-ledger.sh, verify-pretooluse-guard.sh, verify-content-boundary-judgement.sh, verify-workable-items.sh, zero-gap-determinism.sh) all parse correctly; the 2 live checks' REAL --prove-failure output re-piped through parse_count gives 1 and 15 (exact). ONE NEW non-blocking false negative found: '(5 assertions)' with leading punctuation fails (awk token must start with a digit); 'assertions: 5,' (trigger-before-number order) also fails but is a pre-existing directional limitation, not new. Grep confirmed this parenthetical style is NOT used anywhere in this repo's actual proof output and is not exercised by either wired live check — same class of defense-in-depth gap rev-t052 already deferred, noted for the record, not gating. False-positive hunt: multi-line input resolves to the later real count, a trigger word appearing twice resolves to the later match, word-boundary checks (notcaught/pre-caught) do not false-match — no new false positive. coverage.tsv confirmed byte-identical after a fresh regenerate+cmp. bash -n clean, no /home/ paths. Both T052 fix rounds now fully accepted."
