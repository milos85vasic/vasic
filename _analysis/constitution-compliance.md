# Constitution Conformance Audit — Anti-Bluff Compliance Matrix

**Auditor:** Independent constitution-conformance auditor (read-only).
**Date:** 2026-08-05.
**Constitution:** `submodules/constitution/Constitution.md` (working ruleset §11.4, ~165 mandates).
**Work audited:** `design-system/`, `_tools/{od,gen,translate,pdf,portfolio}`, `_content/` + `_content_de/`, `vasic.digital/` + `milosvasic.ru/`, `milosvasic.ru/downloads/*.pdf`, `_tests/` (visual, export, tests, evidence/), `_analysis/`, git state.

**Anti-bluff rule applied:** no verdict is recorded without a checked basis (file read, artifact inspected, or command run). Every COMPLIANT cites a concrete artifact; every GAP/PARTIAL names the specific defect and remediation.

**Headline counts:** COMPLIANT 8 · PARTIAL 5 · GAP 5 · N/A 1.

---

## GAP / PARTIAL first (sorted by severity)

| Mandate | Applies? | Verdict | Evidence / Gap | Remediation |
|---|---|---|---|---|
| **§11.4.161** rootless containers / no sudo | Yes | **GAP** | `milosvasic.ru/start.sh:3` still runs `sudo bundle install && bundle exec jekyll serve`. The sudo the task flagged is **still present**. This is also the one live **§12 host-session-safety** concern (privilege escalation in a run script). No sudo elsewhere (`grep -rn sudo _tools vasic.digital milosvasic.ru` returns only this line). | Remove `sudo`; use a user-level Ruby/Bundler (`bundle config set --local path vendor/bundle`) or run Jekyll in a rootless container. Add a self-test that greps run scripts for `sudo` and fails. |
| **§11.4.77** reproducibility from clean clone | Yes | **GAP** | `.gitmodules` maps **only** `submodules/constitution`. But `git ls-files -s` shows **three** gitlinks (mode 160000): `milosvasic.ru`, `vasic.digital`, `submodules/constitution`. The two site gitlinks have **no `.gitmodules` entry** → `git submodule status` aborts (`fatal: no submodule mapping found ... for path 'milosvasic.ru'`). A clean clone cannot populate the two sites. Also the translation engine defaults to an **ephemeral** path: `HELIX_BIN=${HELIX_BIN:-/tmp/helixtranslate}` (`_tools/translate/translate-content.sh:47`) — present now (15 MB binary in `/tmp`) but not in-repo, so re-translation is not reproducible from a clean clone. | Add `[submodule]` blocks for `milosvasic.ru` and `vasic.digital` (with URLs) to `.gitmodules`, or de-gitlink and vendor them. Ship HelixTranslate via the committed container recipe (`_tools/helixtranslate-container.sh`) and point `HELIX_BIN` at a repo-relative built artifact, not `/tmp`. |
| **§11.4.28** submodules-as-equal-codebase + decoupling | Yes | **PARTIAL** | Constitution submodule is correctly wired (`.gitmodules` → `git@github.com:HelixDevelopment/HelixConstitution.git`, SSH). But the two site submodules are gitlinks without mapping (same defect as §11.4.77) — the submodule graph is internally inconsistent. | Same as §11.4.77: register both site submodules properly so each is an independently clonable, decoupled codebase. |
| **§1.1** false-positive immunity (mutation-paired gates) | Yes | **PARTIAL** | **Proven for 2 of 3 gates.** Visual oracle (§11.4.170) and export validator (§11.4.168) are genuinely mutation-paired: `_tests/visual/self-validate.sh` and `_tests/export/self-validate.sh` each run golden-GOOD (expect PASS / rc 0) and golden-BAD (expect FAIL / rc 1) and assert **both**; evidence verdict JSONs confirm good=PASS, bad=FAIL with concrete findings (`_tests/evidence/harness/{visual,export}/golden-*.verdict.json`). **Missing:** the portfolio data gate `_tools/portfolio/validate.mjs` has **no golden-bad mutation test** — nothing feeds it a deliberately-broken `portfolio.json` (leaked private repo, excluded slug, non-monotonic order) and asserts exit 1. Its immunity is asserted in code comments but never demonstrated. | Add `_tools/portfolio/self-validate.sh`: run validator on a known-good fixture (expect 0) and on a mutated fixture per each assertion class (expect 1); capture verdict evidence. |
| **§11.4.169** mandatory test-type set | Yes | **PARTIAL** | **Present:** e2e/feature (`_tests/tests/*-features.spec.js`, `*.spec.js`), accessibility (`milosvasic-ru-a11y.spec.js`, axe-core), i18n completeness (`i18n-completeness.spec.js`, `*-i18n-articles.spec.js`), visual-proof (`_tests/visual`), exported-doc (`_tests/export`). Playwright reported 51/51 green (commit `42440cd`). **Absent:** unit, integration, security, ddos, stress, chaos, concurrency, memory, perf, challenges, helixqa. Many are arguably N/A for two static sites with no server/runtime, but there is **no document mapping each required type to a PASS or an explicit justified N/A**, so the set is unmet on its face. | Add a short test-type applicability matrix (which types apply to a static-site + PDF + i18n project and why the rest are N/A). Add at least unit tests for the JS tools (`validate.mjs`, `visual-oracle.js`, `validate-pdf.js`, `glossary_protect.py`) and a security/link-safety scan. |
| **§11.4.162** OpenDesign mandatory tokens, no ad-hoc CSS | Yes | **PARTIAL** | **Strong core:** two brand stylesheets share one token foundation with full light + dark overrides and `.od-*` components (`design-system/brand-*/*.css`, `README.md`); sites link the OD CSS (`vasic.digital/index.html:9`). **Ad-hoc CSS remains:** inline diagram `<svg><style>` blocks hardcode hex instead of `var(--od-*)` — e.g. `vasic.digital/products/helixagent.html:68` `svg{--ink:#1e293b;--accent:#a31e39;...}`; `index.html` carries a `<style>` block plus **36 inline `style=` attributes**. Brand mismatch: the **company** site's diagrams use the **personal** brand accent `#a31e39` (crimson) instead of company red `#dc3545`. | Tokenize diagram palettes from `--od-*` (or generate diagrams from OD tokens); remove inline `style=` in favor of utility classes; fix the company-site diagram accent to the company ramp. |
| **DE localization content quality** (sink of §11.4.140/141) | Yes | **GAP (content)** | The independent-review **gate works** (that is compliant — see §11.4.141), but it **fails the shipped German content**: `_tests/evidence/translate-new/de/HelixTrack.md.review.json` and `vasic-digital.md.review.json` both report `"verdict":"FAIL"`, `"untranslated_leftovers":true`, completeness 3/5, with issues "incomplete translation of the tech stack section", "missing translation of the 'Status & honesty notes' section". So `_content_de/` is objectively incomplete by its own reviewer. | Re-run `_tools/translate/translate-content.sh de …` until every `*.review.json` is `verdict:PASS`; do not treat `_content_de` as delivered while its reviewer says FAIL. |
| **§11.4.142** every change independently reviewed (different model) | Yes | **PARTIAL** | Translations are independently reviewed by a **different** model (see §11.4.141). A "comprehensive review complete (89% PASS)" is referenced in commit `42440cd` and `_analysis/` reports. But there is **no per-change reviewer-model attribution** covering the design-system, site markup, and tooling changes — only the translation surface has machine-checkable independent-review artifacts. | Record an independent-review artifact (reviewer model/provider + verdict) per non-translation change class, or a signed review log. |
| **§11.4.107** WCAG contrast, verified independently of tokens | Yes | **PARTIAL (compliant where run)** | Verified **independently of token values** via axe-core, which computes contrast from **rendered pixels**: `_tests/tests/milosvasic-ru-a11y.spec.js` runs `AxeBuilder` and asserts no critical/serious violations (`color-contrast` is a serious-impact axe rule and is **not** in the disabled list). README documents a status-badge contrast fix. **Limitation:** the axe scan targets only the `milosvasic.ru` homepage (`localhost:8082`); it does not cover `vasic.digital`, product/portfolio pages, or the PDFs. No standalone contrast tool exists (`find _tools -iname '*contrast*'` empty), so axe is the sole oracle. | Extend the axe scan to `vasic.digital` and key product/portfolio pages (and ideally a contrast check on PDF text) so coverage matches the "verified/fixed" claim. |

---

## Genuinely COMPLIANT, with proof

| Mandate | Applies? | Verdict | Evidence |
|---|---|---|---|
| **§11.4.170** device-independent host-rendered pixel proof per screen × state × {light,dark}; token-equality-only forbidden | Yes | **COMPLIANT** | `_tests/visual/visual-oracle.js` renders **real HTML in a headless browser**, captures a PNG + OCR text + a geometry oracle (giant-widget/offscreen-clipped/overlap findings) **per theme (light+dark)**, and derives verdict from findings — not from token equality. Real-page evidence: `_tests/evidence/pages/SUMMARY.json` (10 product pages × 2 brands × 2 themes, `allPass:true`), `_tests/evidence/homepages/oracle-{milos,vasic}/*.verdict.json` (findings `[]`, PASS), `_tests/evidence/portfolio/*.verdict.json` (PASS), plus manual desktop/mobile/dark PNGs (6 files). |
| **§11.4.168** exported-doc content + textual + visual validation (PDFs) | Yes | **COMPLIANT** | `_tests/export/validate-pdf.js` runs three families: CONTENT (word count + required phrases via pdftotext), TEXTUAL (no raw markup/mermaid leak), FULL-VISUAL (pdfimages embedded-image count + OCR of rendered pages). Real deliverables pass with captured artifacts: `_tests/evidence/pdf/cv.verdict.json` (809 words, phrases "Harness"/"Singidunum", 4 OCR pages, PASS), `portfolio.verdict.json` (867 words, PASS), `cover-letter.verdict.json`. Self-validation proves the validator can fail (golden-bad rejected on faithfulness, no-leak, and images). |
| **§11.4.69** sink-side positive evidence captured | Yes | **COMPLIANT** | Rendered PNGs, OCR `.txt`, oracle verdict JSON, and PDF verdict JSON are all persisted under `_tests/evidence/{homepages,pages,portfolio,pdf,diagrams,harness}` — the observable output of each sink is captured, not merely asserted. |
| **§11.4.140** all translation via HelixTranslate + fail-loud | Yes | **COMPLIANT** | `_tools/translate/translate-content.sh` routes translation **only** through `$HELIX_BIN` (line 130), runs under `set -euo pipefail` (line 39), pre-checks the engine is executable (`[ -x "$HELIX_BIN" ] || die` line 64), and `die`s with no fallback on engine failure (line 136: "no fallback, no hand-translation"). Engine binary present. (Repro caveat tracked under §11.4.77.) |
| **§11.4.141** independent review by a different model | Yes | **COMPLIANT** | Translator = `mistral/mistral-large-latest`; reviewer = `groq/llama-3.3-70b-versatile` — a different provider **and** model. `_tools/review_translation.py` docstring mandates "a different provider/model than the one that produced the translation." Review artifacts carry attribution: `_tests/evidence/translate-new/de/*.review.json` include `"_provider":"groq","_model":"llama-3.3-70b-versatile"`. The gate honestly emits FAIL (see DE-content GAP), proving it is not rubber-stamping. |
| **§11.4.6** no-guessing / honesty flags / UNVERIFIED markers | Yes | **COMPLIANT** | Explicit uncertainty markers exist: `_analysis/OPENDESIGN-ADOPTION-PLAN.md:157` carries `[UNCONFIRMED — verify OpenDesign icon/asset support …]`; German content includes dedicated "Status & honesty notes" sections (flagged by the reviewer); review JSONs record honest FAIL verdicts and specific issues rather than asserting success. |
| **§11.4.27** no fakes beyond unit | Yes | **COMPLIANT** | Oracles/validators operate on **real** rendered output — real headless-browser renders, real generated PDFs, real OCR (Tesseract). No mocked sinks. The only synthetic inputs are the golden good/bad fixtures, used solely to self-validate the harness (not to stand in for product behavior). |
| **§11.4.10** credentials — no secrets in VCS | Yes | **COMPLIANT** | `git grep` for `sk-…`, inline `API_KEY=…`, and private-key headers over tracked files returns nothing. `.gitignore` lines 6–16 exclude `.env`, `*.env`, `api_keys.sh`, `**/api_keys.sh`, `secrets.sh`. Keys are sourced at runtime from `~/api_keys.sh` (`translate-content.sh`), never stored. |

---

## Not applicable / notes

| Item | Verdict | Basis |
|---|---|---|
| Commit discipline — Conventional Commits | **N/A (not mandated)** | `grep -ni "conventional commit" Constitution.md` returns **nothing** — Conventional Commits is **not** a constitutional requirement. Observed commits are free-form ("Bump milosvasic.ru submodule…"), which is permissible. The constitution instead mandates a **dedicated commit/push script** with hook-validation as a named stage and an always-unblocked push (§11.4.234, Lava binding `scripts/commit-push-all.sh`); **no such wrapper exists in this repo** (minor process gap, not a Conventional-Commits gap). SSH-only transport **is** satisfied: all remotes are `git@github.com:milos85vasic/vasic.git`. |
| GitHub + GitLab baseline releases | **UNVERIFIED (offline)** | `git tag` is empty locally; release existence on GitHub/GitLab could not be checked read-only offline. Not counted as compliant or gap — flagged as unverifiable from the clone. |

---

## MUST-FIX BEFORE DEPLOY

1. **§11.4.161 / §12 — remove `sudo` from `milosvasic.ru/start.sh:3`.** Concrete, single-line privilege-escalation violation; add a run-script sudo-grep self-test to keep it out.
2. **§11.4.77 / §11.4.28 — repair the submodule graph.** Add `.gitmodules` entries for `milosvasic.ru` and `vasic.digital` (both are gitlinks with no mapping → `git submodule status` fatal → clean clone cannot reproduce the sites).
3. **DE content — re-translate until every `_content_de/*.review.json` is PASS.** Two files currently `verdict:FAIL` with `untranslated_leftovers:true`; German is not deliverable while its own independent reviewer rejects it.
4. **§1.1 — add the portfolio validator mutation test.** The data gate `_tools/portfolio/validate.mjs` is the one untested-for-false-positives gate; ship a golden-bad fixture proving it exits 1.
5. **§11.4.77 — de-`/tmp` the translation engine.** Point `HELIX_BIN` at a repo-built artifact (via the committed container recipe) so re-translation reproduces from a clean clone.

## SHOULD-FIX (quality, not deploy-blocking)

6. §11.4.162 — tokenize inline diagram CSS and remove the 36 inline `style=` attributes; fix the company site's diagram accent (`#a31e39` crimson → `#dc3545` red).
7. §11.4.107 — extend the axe contrast scan beyond the `milosvasic.ru` homepage to `vasic.digital` and product/portfolio pages.
8. §11.4.169 — publish a test-type applicability matrix (apply/justified-N/A per required type) and add unit tests for the JS/Python tools.
9. §11.4.142 — record an independent-review artifact (reviewer model + verdict) for non-translation change classes.
